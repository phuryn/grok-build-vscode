# CLAUDE.md — grok-build-vscode

VS Code sidebar extension for **xAI's Grok Build CLI**, driven by `grok agent stdio` over the [Agent Client Protocol (ACP)](https://agentclientprotocol.com). Thin client — all session state, MCP servers, subagents, memory, and plan-mode bookkeeping live in the CLI.

## Status

See [CHANGELOG.md](CHANGELOG.md) for the current version and full release history (the Marketplace/GitHub-published build can lag the local working tree). The whole test suite is **grok-free** — CI never spawns the binary; grok-dependent probes live separately in `research/*.cjs`. Plan mode is **enabled** and enforced client-side (see `research/plan-mode.md` § Resolution). Smoke-tested end-to-end on Linux, Windows-via-WSL, and the **native Windows build** (`irm https://x.ai/cli/install.ps1 | iex`) — `cli-locator` resolves `grok.cmd`/`grok.exe` and `terminal-manager` runs the agent's commands under PowerShell on Windows (`pwsh`→`powershell`, mirroring the standalone CLI; #46), `/bin/sh` elsewhere.

This file is the project map: it describes the **current** architecture, not a per-version log.

**Editing discipline — keep this file lean.** When behavior changes, *rewrite* the affected lines to the new current state; do **not** append a note about what changed or why the old way was wrong. Concretely, none of these belong here:
- version-by-version history or "fixed in 0.2.x / removed in 1.4.x" → [CHANGELOG.md](CHANGELOG.md);
- bug post-mortems / "used to… / this broke because…" narrative → a code comment or `research/*.md`;
- evidence dumps ("one capture: 17.7s", "a real case: 74 messages") → `research/*`.

State the rule, name the function / constant / config-key an agent would grep for, and link the deep-dive — **one fact, one home**. Every feature already has a `research/*.md`; `docs/architecture.md` holds the event-flow detail; [docs/ACP-feedback.md](docs/ACP-feedback.md) is the upstream-facing summary of grok-CLI/ACP friction, agent-family wire differences, and our workarounds (update it when a new workaround lands or an old one becomes obsolete). If a paragraph explains *how we found* a bug rather than *how the code behaves now*, it's in the wrong file.

**Grok CLI compatibility (#22).** A since-fixed Windows-only `grok agent stdio` regression (the agent didn't read stdin until EOF, so ACP startup hung — *"Grok exited (code null)"* / *"ACP request timed out"*) is worked around by pinning Windows to a supported build. The guards live in [src/cli-locator.ts](src/cli-locator.ts) and are all **Windows-only no-ops off win32**: `isStdioBrokenGrokVersion` flags the broken version range, `maybePinBrokenCli` pins those builds to `GROK_STDIO_DOWNGRADE_TARGET` (the single-source-of-truth supported build) before spawning, and `shouldReactivelyDowngrade` → `downgradeBrokenCli` is the evidence-driven backstop that downgrades + retries once on an observed startup failure above the target. When a newer Windows-verified build ships — **verify with the session/new probe, not just initialize** — bump the constant and widen the broken range. Exact versions, diagnosis, and probes: `research/stdio-eof-regression.md`; timeline: [CHANGELOG.md](CHANGELOG.md).

## Module map

| File | Role |
|---|---|
| `src/extension.ts` | Entry point — registers commands, keybindings, output channel |
| `src/sidebar.ts` | Webview provider, message routing, fs handlers, native diff editor opening (whole-file expansion delegated to `diff-view.ts`), `logout`, generated-media inlining (`postGeneratedMedia`), and the active-editor context chip (`refreshImplicitChip` mirrors the open file + live selection range, applies `grok.includeActiveFileByDefault` live). **Eye-off on that chip is a standing preference, not a per-file one** (#67): the chip is rebuilt on every file switch, so the choice is persisted to `IMPLICIT_CHIP_HIDDEN_KEY` in globalState on `toggleChip` and re-seeded by the pure `implicitChipStartsHidden(prev, remembered)` in `chips.ts` — the live chip wins when one exists (globalState writes are fire-and-forget), the store seeds a fresh webview/restart. Without it, dismissing the context was futile: the next file switch silently re-enabled it. Every attach flow (Send Selection/File, @-mention, the `+` picker, image paste) routes through `revealAndFocusComposer` → the `focusInput` host message, landing the caret in the composer so the user can type immediately (#43); `insertActiveMention` degrades a Command-Palette Send File with no active editor into the file picker instead of a silent no-op |
| `src/diff-view.ts` | Pure native-diff reconstruction (#66) — `expandDiffToWholeFile` combines Grok's region-only diff block with the current disk file, using `_meta.details[]` site lines / the pre-write echo line and explicit `replace_all` semantics to avoid phantom matches; `firstChangedLine` positions the editor on the first hunk; `MAX_DIFF_EXPAND_BYTES` bounds memory. Unreadable, stale, or oversized files fall back to the region-only diff |
| `src/acp.ts` | ACP client — spawns CLI, manages session lifecycle, emits events (incl. `mediaContent` from `emitToolMedia`). `interject` (#52), `forkSession` (#48), and the worktree RPCs (`createWorktree`/`listWorktrees`/`applyWorktree`/`removeWorktree`, P2-8) call the **unadvertised** `_x.ai/interject` / `_x.ai/session/fork` / `_x.ai/git/worktree/*`; all return `"unsupported"` (never throw) on -32601 so an older CLI degrades instead of erroring. `PromptResultMeta`/`PromptUsage` are **re-exported** from `acp-dispatch`, not redeclared — the duplicate silently dropped fields added on one side |
| `src/worktree.ts` | Pure worktree helpers (P2-8) — parse create/list/apply/remove/status payloads, path equality, `worktreeDisplayName`, multi-cwd session-index merge (`mergeSessionIndexes`/`collectSessionCwds`); see `research/worktree.md` |
| `src/rewind.ts` | Pure rewind helpers (P2-9) — parse `_x.ai/rewind/points` + `execute`, selectable targets, confirm/label formatters; see `research/rewind.md` |
| `src/run-progress.ts` | Pure Deep Research / Workflow / Goal progress parsers (P2-10) — `isRunProgressUpdate` / `parseRunProgressUpdate` / `workflowControlCommand` from the live `_x.ai/session_notification` rail; see `research/run-progress.md` |
| `src/acp-dispatch.ts` | Pure protocol helpers — line parsing, update routing, response builders, generated-media extraction (`isMediaGenToolCall`/`extractGeneratedMediaPaths`), context-donut gates (`gateZeroTokenMeta`, `parseSessionInfoContext`), the live `_x.ai/session_notification` consumers (`contextUsedFromCompactNotification`, `autoCompactStartedNote`, `isSubagentLifecycleUpdate`), and error classifiers `isIncompatibleAgentError` + the turn-failure triage family (#57/#58) with three tiers, mirroring the CLI's own error contract (OSS `sampling/error.rs`): **(1)** `isRateLimitError`/`rateLimitNoticeText` — a `RATE_LIMITED_ERROR_CODE` (-32003) or limit-phrased failure renders "Usage limit reached — not a sign-in issue", never auth recovery; **(2)** `isAuthErrorText` — the deliberately-broad gate for ONE guarded reload+resend (`recoverAuthAndResend` in `sidebar.ts` restarts the wedged pool process so a fresh one re-reads `~/.grok/auth.json`, once per streak via `Session.authRecoveryTried`, which deliberately survives `startSession`); **(3)** what the resend's failure SHOWS is decided by `isCredentialError` (the `AUTH_REQUIRED_ERROR_CODE` -32000 the CLI stamps on every genuine credential failure, or strict credential wording — no 403/forbidden/billing/bare-"api key") → only that opens the sign-in overlay (posted live-only, after an `agentError` that closes the turn so "Grokking" can't stick); billing/entitlement wording a fresh process couldn't clear gets `entitlementNoticeText` in-chat instead (the 403 "requires a Grok subscription" can't be fixed by login — and carries the CLI's own "cached OAuth session shadows XAI_API_KEY → `grok logout`" hint verbatim). `promptErrorText` is the shared one-stop surface (limit notice / entitlement notice / raw `errorDetail`) |
| `src/protocol.ts` | **Single source of truth for the host↔webview message contract.** Two discriminated unions — `HostMsg` (host→webview) + `WebviewMsg` (webview→host) — plus exhaustive `Record<Union["type"], true>` maps that compile-time-force the runtime `HOST_MESSAGE_TYPES`/`WEBVIEW_MESSAGE_TYPES` arrays to equal the unions' discriminants. `sidebar.ts` types `post`/`emit`/`Session.buffer` against `HostMsg` (the host→webview half used to be `any` — the "post one shape, handle another" gap around restore/pagination/media); the webview keeps a mirror of the type arrays in `webview-helpers.js` and `test/protocol.test.ts` asserts TS↔JS set-equality + that `chat.js` handles every host type. All type imports are `import type`, so this module carries no runtime deps and the test can load it without vscode. Typing it caught two latent mismatches (`session.currentModelId` can be undefined; `exitPlanRequest.req` carries `planPath`/`planName`) |
| `src/session.ts` | Per-session state bag (`Session`) — one per live `grok agent stdio` process; the sidebar holds a *pool* of these + one `focused`; `buffer` is typed `HostMsg[]`; `lastTurnUsage`/`sessionUsage` are the billing split + OUR cumulative total (#53); `queuedSends` is the host-owned pending send (#37) — a **single entry**: composing more while one is queued **appends** to it (blank-line separator, the exact flush format), flushed by `maybeFlushQueuedSends` (sidebar.ts) when the session's turn truly ends (guards: working/needs-you/afterTurn/priming) — **backgrounded sessions flush too**, and the process-exit path drops it |
| `src/session-pool.ts` | Pure reaping policy (`selectReapable`) — idle-TTL + LRU cap over the live-session pool (never the focused/working/needs-you) |
| `src/cli-locator.ts` | Locate `grok` binary (configured path → `~/.grok/bin/grok` → PATH); cross-platform |
| `src/terminal-manager.ts` | Headless shell children for the agent's `terminal/*` ACP calls. The **shell is the host's choice** (grok delegates every command over ACP, it never runs them itself), so on Windows `resolveTerminalShell` runs them under PowerShell — `pwsh.exe`→`powershell.exe`→cmd.exe fallback — to match the standalone CLI (profile functions + pipelines that cmd couldn't run; #46); `/bin/sh` (`shell:true`) elsewhere. Resolution is cached per-process (one `where` probe) |
| `src/chips.ts` | File-chip CRUD (pure) + vision-input policy: `isVisionImagePath`/`isVisionMime` whitelist (png/jpe?g/gif/webp — SVG deliberately stays a *path* chip so grok can read/edit the source; BMP excluded), single `EXT_MIME` table deriving `mimeFromPath`/`extFromMime`, `MAX_VISION_IMAGE_BYTES` (20MiB, the documented xAI cap), `makeImageChip` (session-scoped `[Image #N]` index + `originRelPath` for disk imports) |
| `src/prompt-builder.ts` | Chip → prompt payload (pure): whole-file chips become a plain path list (not `@`-reads), **split by origin** into **"Attached file(s):"** (explicit intent) vs **"Currently open in the editor (for context):"** (ambient, `isImplicitChip`); selected-range chips become fenced blocks. File-path context rides a machine-readable `<vscode-context>` envelope (`CONTEXT_TAG_OPEN`/`CLOSE`) parsed back on restore. A confirmed `slashCommand` (from `matchSlashCommand`) must stay at position 0, so the envelope + snippets TRAIL the command text. `buildPromptWithImages` appends host-pre-read `{type:"image"}` blocks + trailing `[Image #N]` tags (with a do-not-Read hint); no images → byte-identical to `buildPrompt`. See `research/compact.md`, `research/vision-input.md` |
| `src/slash-filter.ts` | Slash-command autocomplete filter + `matchSlashCommand` (pure) — the dispatch gate: token shape `^\/([A-Za-z0-9][\w.:-]*)(?:\s|$)` (rejects Unix paths like `/tmp/foo`) validated against the CLI's advertised `availableCommands` (shape-only before the list arrives). Also `filterAdvertisedCommands`/`HIDDEN_SLASH_COMMANDS` — drops commands that misbehave over ACP at ingestion so they're absent from both autocomplete and dispatch: the config-mutating no-op `/always-approve` (#31) and `/context`, which renders only in the CLI's own TUI (#39; `/session-info` is the working equivalent) |
| `src/mention.ts` | The composer's `@` file popover, host half (pure) — `filterMentionFiles` ranks the index against the typed token (basename-prefix → basename-substring → path-substring → subsequence; shorter path wins in a tier), `buildExcludeGlob` merges `files.exclude`+`search.exclude` into the findFiles exclude (findFiles' default applies files.exclude ONLY — node_modules lives in search.exclude), `orderMentionIndex` (shallow-first) is the empty-`@` order. The webview half (`getMentionQuery`/`applyMentionPick` — token detection + pick rewrite) lives in `webview-helpers.js`; the host caches one findFiles snapshot per `MENTION_INDEX_TTL_MS` (`mentionFindFilesIndex` in `sidebar.ts`, concurrent keystrokes share one in-flight build) and answers `mentionQuery`→`mentionResults`. **The findFiles cap is the #69 trap:** past it VS Code returns an *arbitrary* subset, so real files vanish from `@` while loose subsequence matches still show — `clampMentionIndexLimit` reads the user-tunable `grok.mentionIndexLimit` (default `MENTION_INDEX_LIMIT` 5000, floor `MENTION_INDEX_LIMIT_MIN` 100, no ceiling; a config change drops the cache), and `mergeMentionEntries` layers the currently-open tabs (`openWorkspaceFileEntries`) over the snapshot **at read time, never into the cache** — returning the same Map instance when nothing is new — so an open file is always mentionable regardless of the cap and closing the tab stops injecting it. A pick posts `addMentionFile`, which attaches the file through the same `addDroppedFile` pipeline as drop/+, while the `@rel/path ` text stays in the composer |
| `src/grok-config.ts` | Reads grok's `config.toml` `[ui] permission_mode` (pure) — `configForcesAlwaysApprove` (project `.grok/config.toml` over global `~/.grok/config.toml`) drives the honest "Auto accept" label when always-approve is set globally (#31) |
| `src/plan-gate.ts` | Plan-mode policy (pure) — workspace-write containment, read-only command allowlist, permission/plan-file classification |
| `src/plan-restore.ts` | Plan persist + restore decision (pure) — appendPlanEntry + decideRestoreState + `countsAsUserBubble` (the host's replay counter must count exactly what the webview bubbles, or post-restore verdict positions inflate and their cards land at the end) |
| `src/grok-primer.ts` | Hidden plan-mode primer text + version/marker constants + pure `isPrimerText()` (detects the primer when grok replays it on restore, so it's hidden + not counted toward plan positions) + `isPrimerSummary()` (matches grok's primer-derived session titles — cheap pre-filter for the empty-session sweep) |
| `src/sessions.ts` | Disk-driven session listing/delete + customName overrides + **empty-primer-session detection** (`extractUserQueries`/`classifyUserQueries`/`isEmptyPrimerSession`, all pure) — reads grok's `chat_history.jsonl` to confirm a session got the primer and no real query. Also `discoverRepos` (pure) — the repo catalog behind the remote switcher — and `readContextUsage` (pure) — reads a session's `signals.json` for the context-donut seed/refresh (see § ACP surfaces → `_meta.totalTokens`). `resolveGrokHome` resolves grok home the way the CLI does (`GROK_HOME` override, else USERPROFILE-first on Windows / HOME elsewhere — the CLI's Rust `home_dir()` ignores HOME on Windows); `isPathInside` is the segment-boundary check behind serve-from-disk media |
| `src/file-ref.ts` | Open-file ref parsing (`path#L<n>`, `path:line[-end|:col]` — suffixes end-anchored so drive colons survive) + `fileUriToPath` (`file://` → fs path, incl. Windows drive-letter + UNC forms; backs `refFromUri` and the drag-drop flow) + large-file inline-read guard (pure) |
| `src/file-upload.ts` | Pure remote-document upload boundary — basename sanitization, `.md`/`.txt`/`.pdf`/`.csv`/`.xlsx`/`.docx` allowlist, strict base64/size validation, owned UUID staging-path checks, and session/fork reference accounting |
| `src/plan-review.ts` | Plan-snapshot Markdown filename generation for the "open plan as editor tab" action (pure) |
| `src/mode-prefs.ts` | Remembered-mode policy (pure, #25) — `modeToRemember` (persist Agent/Auto-accept, never Plan) + `startsInYolo` (new-session-only application) |
| `src/view-move.ts` | View placement (pure) — the view is **default-homed in the Secondary Side Bar** (`viewsContainers.secondarySidebar`, hence the `engines` floor of `^1.106`; Cursor 3.x is on base 1.125). package.json contributes one extension-owned container per dock location (`grokSidebar` = secondary side bar, homes `grok.chat`; `grokPrimary` = activity bar; `grokPanel` = panel — empty containers render nothing), so the gear → Config & debug **"Move view"** items relocate the view **directly** via the internal `vscode.moveViews` command (the one GitLens uses) with no quickpick; `moveViewContainerFor` maps the webview's `{type:"moveView", location}` to the container id, and an unknown location falls back to `workbench.action.moveFocusedView` *with the view id argument* (Cursor never sets the `focusedView` context for webview views, so the argument is what makes it work). Exists because Cursor's primary-side-bar context menu hides the built-in "Move To" entry |
| `src/telemetry.ts` | Anonymous Aptabase telemetry — pure builders (`aptabaseHost`/`osNameFromPlatform`/`shouldSendTelemetry`/`buildSessionStartEvent`) + a fire-and-forget `postEvent`. One `session_start` per session on the first real user message; no SDK |
| `src/remote-policy.ts` / `src/remote-frames.ts` / `src/remote-uplink.ts` | Remote client for the AFK Pilot relay — the relay URL is the `REMOTE_RELAY_URL` code constant in `remote-frames.ts` (no user setting; edit the constant for a local build). Pair via `Grok: Link Remote Device` or gear → AFK Pilot → Sign in; unlink via the palette command or gear → Sign out. `remote-policy` (pure) classifies every protocol message and gates what a remote may send (worktree/rewind + link actions are `host-local` — never remote-drivable); `remote-frames` (pure) is the relay wire contract; `remote-uplink` dials out over ws. Until a device is linked, nothing runs |
| `src/keep-awake.ts` | OS wake lock held for exactly the uplink's lifetime (device token stored + extension host alive), so an AFK machine can't idle-suspend mid-turn — `caffeinate -i -s -w` (macOS), a `SetThreadExecutionState(ES_CONTINUOUS｜ES_SYSTEM_REQUIRED)` PowerShell (Windows), `systemd-inhibit --what=idle:sleep --mode=block` (Linux), no-op under WSL. Pure plan builders (`buildKeepAwakePlan`/`shouldKeepAwake`/`keepAwakeFallbackWhat`) + the `KeepAwake` runner; `refreshKeepAwake` in `sidebar.ts` re-asserts it on link/unlink/dispose/config change. **Display sleep is deliberately NOT blocked**, and every failure is log-only — never a notification, a throw, or a retry loop. `grok.remote.keepAwake` (default true) is the opt-out; see `research/keep-awake.md` |
| `src/voice.ts` | Voice-input pure helpers — STT request/response/error, per-platform ffmpeg args, DirectShow device parsing, API-key resolution. `resolveVoiceKey` order: `grok.voiceApiKey` → `GROK_VOICE_API_KEY` → `XAI_API_KEY` → `authKey` (the `grok login` token). `extractGrokAuthKey` reads that token from `~/.grok/auth.json` (#51) — xAI-issuer-only, refuses a known-expired entry (skew + numeric/ISO expiry parse), so Voice works signed-in with no separate key; disclosed in `docs/privacy.md`, setup in `docs/voice-setup.md` |
| `src/voice-recorder.ts` | Batch capture: `VoiceRecorder` (spawns `ffmpeg` → WAV, graceful `q`-stop) + `transcribeAudio` (POST to `api.x.ai/v1/stt`) + `resolveWindowsAudioDevice` |
| `src/voice-streamer.ts` | Live capture: `VoiceStreamer` (ffmpeg PCM → `ws` → `wss://api.x.ai/v1/stt`, emits partial/final transcript events). The webview snapshots the composer selection into `voiceBefore`/`voiceAfter`, so partial and final transcripts insert at the restart cursor (or replace a selection) while preserving the sentence suffix. A manual Send/Queue posts `voiceStop { discard:true }`: `stopVoiceForManualSend` clears the webview mic state and rejects in-transit voice events, while `stopVoiceInput` increments `voiceGeneration` so late ffmpeg/WebSocket/STT completions cannot refill the composer. Spoken `grok send` deliberately keeps the continuous stream behavior |
| `media/chat.{js,css}` | Webview UI — chat rendering (markdown, LaTeX via MathJax, Mermaid), inline generated media, math/diagram export, history popover. RTL content: `applyAutoDir` stamps `dir="auto"` on every markdown block element, `unicode-bidi: plaintext` on prose containers covers loose paragraphs (renderMarkdown emits them bare + `<br>`, never `<p>`), code/pre pinned LTR, composer + its highlight mirror `dir="auto"` — content direction only, the chrome never mirrors. Tool calls grouped per batch + summarized by ACP **kind** (`categorize`/`summarizeTools`/`titleKind` — "Explored N items"/"Edited N files", not "Ran N commands" for reads); narration **interleaves** with its tool groups (`addToToolGroup`). Codex-styled tool/thinking rows, one lucide category icon per row (`toolIconFor`). Thinking traces **hidden by default** (`grok.showThinking`, #26) behind a `thinking-hidden` body class, with a **"Thinking…"** stand-in + `ensureActivityIndicator` (always some live-turn progress affordance); floating **scroll-to-bottom** button (#28) in `.composer`. Send/Stop (`state.busy`) is **event-sourced per session** — `agentStart` sets it, `agentEnd`/`agentError`/`exit` clear it, `clearMessages` resets it — so a focus swap/replay lands on the true state; **typed text never cancels** (#37, Claude-style): while busy, Enter *and* the button click queue the message (`queueFromComposer` posts `queueSend` — the queue is **host-owned per session**, see `session.ts`), the pending message renders as **ONE user block pinned to the end of the chat** (`renderQueuedBlocks` — italic, dashed border, clock tag; Edit pulls the whole text back to the composer, Remove drops it, both via `dequeueSend`) rebuilt from `queuedSends` snapshots so it **survives focus switches**, the button face tracks the composer (text → queue-send arrow, empty → square Stop, locked startup → disabled spinner with no cancel), and clicking Stop hands queued text back to the composer + posts `clearQueuedSends` before the cancel (a halt must not auto-fire queued sends). See § ACP surfaces |
| `media/webview-helpers.js` | Pure webview helpers shared with tests — file-ref detection, relative-time, mic-button state machine, trailing send-phrase highlight, the `@`-mention composer half (`getMentionQuery` token detection / `applyMentionPick` pick rewrite — see `src/mention.ts`), subagent classifier (`isSubagentToolCall`/`subagentLabel`), failed-tool reason (`toolFailureText`), and the restore parsers that peel `buildPrompt`'s shapes back into chips (`parseAttachmentContext`, `parseSelectionBlocks`, `parseImageTags` — each strips only the leading/trailing shapes we emit, so a literal match mid-message survives restore) |
| `scripts/install.{ps1,sh}` | Auto-detect a code-compatible CLI (`code` → `code-insiders` → `cursor` → Antigravity), build .vsix, install; optional CLI arg / `-Cli` / `CODE_CLI` targets a specific IDE; `-All`/`--all` installs into **every** detected known IDE in one run (build once) |
| `scripts/uninstall.{ps1,sh}` | Uninstall `PawelHuryn.grok-vscode-phuryn` (same optional CLI targeting + detect chain) |

Pure modules (`acp-dispatch`, `protocol`, `chips`, `prompt-builder`, `slash-filter`, `mention`, `grok-config`, `cli-locator`, `sessions`, `plan-gate`, `plan-restore`, `grok-primer`, `file-ref`, `file-upload`, `plan-review`, `mode-prefs`, `view-move`, `voice`, `session-pool`, `worktree`, `rewind`, `run-progress`, `webview-helpers`) were split out specifically so protocol behavior can be unit-tested without spawning processes. (`session.ts` is a plain state bag — no `vscode`/spawn/network either, but it's data, not logic; `protocol.ts` is pure types + two runtime arrays.)

## Session pool (Agent Dashboard)

The sidebar runs a **pool of live sessions** (one `grok agent stdio` process each) with one `focused` (the one rendered). Each session's state is a `Session` (`src/session.ts`); the sidebar holds `focused` + a `Set<Session>` `pool`. **Re-focus is lossless**: every session buffers the webview posts that built its chat (`Session.buffer`), so `focusSession` is `clearMessages` + replay of that buffer — no grok reload, no kill, even mid-turn/mid-approval. Clicking a *cold* session (no live process — reaped or pre-window) loads it from disk into a fresh member (`openSession`); a live one re-focuses instantly. Two guards keep it safe: a **per-session `gen`** (bumped only when *that* session's client is torn down, captured by handlers when wired) so a backgrounded session's in-flight events aren't judged stale when focus moves; and **session-scoped `emit(session,…)`** (buffers + forwards only when focused) vs UI-wide `post(…)` (status dots, sessions list). **Status dots.** Every history row shows a dot whose color is a *derived* value (the pure `computeDot` in `src/session-pool.ts`), not the raw `status`: **gray** at rest (idle / read / cold / loaded-from-disk all collapse to one gray — the user never learns warm-vs-cold), **blue** working, **yellow** needs-you, **green** finished-and-unread, **red** errored-and-unread. The green/red is an **unread badge**, not a live state: a turn that ends while the session is *not* focused sets a persisted `unread`/`unreadError` flag in `SessionMetaOverride` (globalState); focusing/opening the session clears it (`markRead`). So the badge **survives reaping AND reload** (it's meta-backed, decoupled from the live process) — that's the payoff: come back and the green dots are exactly the sessions with results you haven't seen. No timer/decay — a never-opened session stays green (it *is* still unread). `computeDot({liveStatus, unread, unreadError})` precedence: working → needs-you → unread(error?) → none. Dots are pushed one-at-a-time via `pushDot`→`sessionDot` (cheap, no disk read) plus a full `dots` map (`dotForId` over disk entries + pool) on each list refresh; palette in CSS — Working=`--vscode-charts-blue`, Needs-you=`--vscode-charts-yellow`, unread=`--vscode-charts-green`, error=`--vscode-errorForeground`, none/default=`--vscode-descriptionForeground`. **Reaping** is the pure `selectReapable` (`src/session-pool.ts`): an **idle TTL** (untouched ≥1h, swept every 5 min) + an **LRU cap** (~8 live; least-recently-used eligible evicted past it), but it **never** reaps the focused or a `working`/`needs-you` session (so the cap can be exceeded when everything spare is busy, by design). Reaping is silent — kill the process, recompute the dot (an unread reaped session **stays green**; a read one goes gray); re-click reloads from disk. Recency (`Session.lastActiveAt`) is stamped on create/focus/made-busy via `touch`. **CLI-update guard:** the explicit *Update Grok Build CLI* action (`updateGrokCliOnDemand`) confirms before tearing the pool down if any session is `working`/`needs-you`; the silent startup auto-update has nothing in flight by construction, so it doesn't ask. Per-workspace scope. The impure `voice-recorder`/`voice-streamer` (ffmpeg spawn + STT fetch/WebSocket) are smoke-tested manually via the `research/voice-*.cjs` probes (`voice-stt-probe`, `voice-e2e-verify`, `voice-stream-probe`, `voice-stream-verify`, `voice-cost-probe`).

**Empty-primer-session cleanup (#24).** Every time the extension opens it primes a fresh grok session (`ensurePrimed`); if the user never sends a real message that leaves an empty, primer-only session on disk, titled from grok's primer-derived summary ("… Primer v4 Plan Mode …"). Two mechanisms keep those out of history. **(1) Recycle on leave (in-memory, bulletproof):** `parkFocused` knows a session is primer-only via `hasHistory === false` (the primer is sent through `ensurePrimed`, *not* the `send` path, so it never sets `hasHistory`), and on New Session / switching away it deletes that session's on-disk dir (`removeSessionFromDisk`), not just its process — so at most one untitled **New session** ever exists, and `postSessionsList` forces its display name to "New session" while live (in-memory `!hasHistory`), never grok's primer title. **(2) Startup sweep (legacy cleanup, content-confirmed):** `sweepEmptyPrimerSessions` runs once per activation over the newest `SWEEP_SCAN_LIMIT` by mtime; it skips live + renamed (`customName`) sessions, then reads `chat_history.jsonl` and deletes only if `isEmptyPrimerSession` confirms the session got our primer and **zero real user queries** — so a real session (even one with a primer-contaminated title) or a session we didn't start (no primer) is never touched. **The chat-history content check is authoritative regardless of `num_messages`** — `EMPTY_PRIMER_MAX_MESSAGES` only gates the no-content title-heuristic fallback (an agentic primer turn can balloon the count, so it must not veto the content read). Detection is content-based and agent-agnostic: grok keys the role on `type` (not `role`), wraps prompts in `<user_query>` **but not always** (slash commands like `/imagine` arrive UNWRAPPED), and tags injected `<system-reminder>`/context turns `synthetic_reason`; `extractUserQueries` handles all of that, so the same logic works for the `grok-build`/`grok-build-plan` and `cursor` (composer) agents alike.

## History pagination

The history popover loads sessions **one page at a time** (`SESSION_PAGE_SIZE = 100`, newest-first) so it stays responsive with thousands of sessions on disk. The cost of the old behavior (read + `JSON.parse` *every* `summary.json` on every open, then render every row) grew linearly and stalled the popover at scale.

**Scope — the selected repo.** `discoverRepos` (`sessions.ts`, pure) enumerates cwd catalogs under `<grokHome>/sessions`, rejecting temp roots and `<grokHome>/worktrees` (a worktree isn't a checkout you choose between). `postSessionsList` indexes the selected repo **plus the worktrees belonging to it** (`sessionCwdsForRepo` → the pure `worktreeCwdsForRepo` → `mergeSessionIndexes`) — without that merge a worktree session is stranded the moment you leave it. **The primary workspace lists EVERY known worktree, unconditionally**: `sourceGitRoot` is the CLI's *git root*, which is not the workspace folder when a subdirectory is open, so matching is best-effort and must never be why a session vanishes; only a non-primary selected repo filters, by containment. `trustedCwds` (the workspace root) bypasses the managed-worktree exclusion for the same reason — the selection must always name a catalog row, or `clearAllSessions`/`selectRepo` silently no-op. The picker is **remote-only** (`#repo-btn`; in VS Code the window already *is* the repo) and, because the relay serves a client that can outrun the installed extension, the chip appears only once a `repos` frame has arrived — an older host that never sends one shows no chip rather than a dead control. **The selection is global for REMOTE clients only** (that is the feature — one phone drives whichever project you pick, and every remote client agrees), but the VS Code webview ignores it: the switcher is hidden there, so following it would re-scope a history list the user cannot re-aim and point *New session* at a checkout they are not looking at. `repoScopeFor` (`remote-policy.ts`, pure) is that rule; `onMessage` carries a `MsgOrigin`, and `postSessionsList`/`postRepoCatalog` build a payload per audience (`postLocal` / `postRemote`, second scan skipped when both scopes match). Sticky chrome records the REMOTE variant, so a late-joining client replays the right one. Undo the split if VS Code ever grows the switcher. Pins live in `globalState["grok.repoPins"]`; selecting a repo changes history scope only, and *New session* is the deliberate second action that starts grok there. Remote `selectRepo`/`toggleRepoPin`/`clearAllSessions`/`resumeSession` are additionally cwd-gated by `allowRemoteRepoTarget` (`remote-policy.ts`), which takes a **predicate** so the catalog is resolved from disk only for the messages that name a cwd — this gate sees per-keystroke `mentionQuery` traffic.

**Two pure primitives** replace the monolithic full-scan `listSessions` (which is kept only for callers that genuinely need the whole list):

- `indexSessions({fs, grokHome, cwd})` — one `stat` per session dir, **no reads/parses**, returning every id ordered newest-first by `summary.json` **mtime**. mtime is the cheap last-activity proxy: grok rewrites that file (it holds `updated_at`) on every turn. We sort by mtime here *because the id is a UUIDv7 whose timestamp is creation, not last activity* — id-sort would be wrong.
- `readSessionEntries({fs, grokHome, cwd, ids, overrides})` — reads + parses `summary.json` for **exactly the given ids** (the visible page) and applies `customName` overrides. The exact `updated_at` order is re-applied within the loaded page (mtime is only an approximate sort key).

**Host orchestration** (`postSessionsList` in `sidebar.ts`): `indexSessions` orders everything cheaply; a `mtimeById` map drives an **mtime-keyed read cache** (`sessionCache`, `readEntriesCached`) so a re-open / load-more / search reads only entries whose `summary.json` actually changed — steady-state opens cost ~zero reads. It posts `{type:"sessions", entries, activeId, dots, offset, total, hasMore, query}`. **Search** is server-side and complete: a non-empty `query` warms the whole catalog once (cache-backed) and filters by display name across **all** sessions, not just the loaded page. The cache is invalidated on `rename` (changes displayName without touching mtime), `delete`, and `clearAll`. **Live-session synthesis:** a brand-new session has no `summary.json` yet, so on the **first, unfiltered page** `postSessionsList` injects a synthesized `liveSessionEntry` (pinned newest-first) for any live `pool` member whose `activeSessionId` isn't on disk, named from the best in-memory source (generated/renamed `customName` → first user message → `"New session"`); the next refresh replaces it once grok flushes the file. `hasMore` is computed from the on-disk page *before* injection (load-more pages are disk-only), and these ids are never on disk so they can't duplicate onto a later page.

**Webview** (`media/chat.js`): `offset === 0` replaces the list, `offset > 0` appends (de-duped by id); a scroll-near-bottom listener requests the next page (`hasMore`/`sessionLoading` guards keep it to one request per boundary); the search box posts a **debounced** (180ms) `listSessions` with the query; a host-driven refresh that arrives unfiltered while a search is active triggers a **sticky-search** re-request so the filtered view isn't clobbered. The "Clear all" footer shows when any non-active session exists (loaded *or* on a later page, via `total`). **Popover positioning:** `positionDropdownPopover` **right-aligns** the popover to the panel edge (`right: 6px`/`left: auto`) and grows it leftward — width-independent, no measurement. It **caps min/max width to the panel** (`min(360|280, parentWidth − 2·EDGE)`, overriding the CSS `min-width: 280px`/`max-width: 360px`) so a long session name **ellipsizes** in a narrow panel instead of overflowing. **Resize/hide:** a `window` `resize` listener re-fits it live while open; a `document` `visibilitychange` listener `closePopovers()` when the view is hidden (a resize while hidden fires no event, so close rather than reappear stale).

**Perf simulation** (`npm run test:perf`, `test/sessions.perf.ts` via `vitest.perf.config.ts`) — opt-in, out of `npm test`/CI. At N=5000 it asserts the op counts: first open drops file reads from **5000 → 100** (~98%), steady-state re-open is **0 reads** (mtime cache), search warms once then **0 reads**, with a modeled-latency projection (~3250ms → ~310ms) and a real in-memory parse-cost wall-clock (~2.7x, disk-latency-free lower bound).

## Build + test

```bash
npm install
npm test         # 1435 tests, a few seconds, vitest — all grok-free (incl. happy-dom DOM tests + fake-CLI ACP integration tests)
npm run test:perf # opt-in session-history perf simulation (NOT in npm test/CI; see § History pagination)
npm run test:integration # real VS Code Extension Host (~6s warm). NOT part of `npm test`;
                 # CI runs it as a second required job and `release.*` now gates on it too.
npm run package  # → grok-vscode-phuryn-<version>.vsix (clears older *.vsix first)
```

### Test taxonomy — three layers

There are **three** kinds of tests, and it matters which is which:

1. **`npm test` — grok-free unit/DOM/integration suite (1435 tests).** Pure logic, happy-dom tests that drive the real `media/chat.js`, a real-`/bin/sh` TerminalManager smoke, and a fake-CLI ACP integration suite (`test/fixtures/fake-grok-acp.cjs`). **Never spawns the real `grok` binary.** Runs in a few seconds with no network, no login, no subscription. This is the floor — every change keeps it green. (Two separate opt-in suites live outside `npm test`: `npm run test:perf` runs the session-history perf simulation — `test/*.perf.ts` via `vitest.perf.config.ts` — and `npm run test:integration` runs the `@vscode/test-electron` smoke below; both are matched only by their own config so they stay out of `npm test`.)
2. **CI — the *same* suite, plus a REQUIRED Electron smoke.** `.github/workflows/ci.yml`'s `test` job runs `npm ci && npm run compile && npm test && npm run package` on a clean Ubuntu box — **CI's `test` job ≡ layer 1, verbatim** (no `grok` binary, no auth, no subscription, so it *cannot* touch the real CLI — the whole reason layer 1 is grok-free). A second job, `integration`, runs the `@vscode/test-electron` smoke under `xvfb` (`npm run test:integration` — activate the extension, assert commands registered, resolve the webview via the *missing-CLI* onboarding path so it needs no grok). It's a **required gate** (validated passing against a real VS Code Extension Host before landing; if a Linux/xvfb quirk ever surfaces, fix it rather than demoting the gate). The smoke is still grok-free — it exercises host glue (activation, `getHtml`/CSP, `localResourceRoots`, command registration) the unit suite structurally can't reach. Compiles in isolation (`integration/tsconfig.json` → `out-integration/`), so it's out of `npm test` and the extension build.
3. **`npm run test:live` — on-demand pre-release suite against REAL grok (`scripts/live-tests.cjs`).** Spawns the actual `grok agent stdio` and exercises the surfaces layers 1–2 can't: the real ACP handshake, a **capability-drift probe** (`capabilities` — snapshots `promptCapabilities` and hard-asserts the `image:false` baseline, so the day grok flips it the gate FAILs with a "the vision workaround may be removable" message; paired with `vision-prompt` which pins that vision *actually* works, this is an advertised-vs-actual drift detector), a prompt round-trip, a **mid-turn cancel** (`cancel-mid-turn` — the Stop-button contract: `session/cancel` as an id-less notification must settle the in-flight prompt with a cancelled stopReason and leave the session usable, #37), **mid-turn steering** (`interject` — the #52 Steer contract: `_x.ai/interject` must reach the MODEL mid-turn *and* leave the turn uncancelled; an unadvertised RPC, so this is the drift canary that tells us if Steer silently degraded to queueing), **conversation forking** (`session-fork` — #48: a new id, the parent's history carried, loadable; pins `ForkSessionRequest`'s required param shape), **concurrent sessions** (`parallel-sessions` — two `grok agent stdio` processes on one workspace, mirroring the Agent Dashboard pool: overlapping prompts complete independently with no cross-talk), session restore, **plan-mode enforcement modeled as the two real flows** (primer → plan → `[Plan rejected]` keeps the gate up & asserts 0 workspace mutations *and* byte-identical seed files — the containment canary — → `[Plan approved]` drops the gate & asserts implementation can land; the old single-turn test invented an impossible approve+no-verdict+gate-up state that pushed grok-4.5 into force-writing), **rewind + edit over cancelled plans** (`plan-cancel-rewind` — three no-op-plan-then-Cancel rounds, then rewind: asserts the bubble→point map skips the plumbing turns those rounds create (the hidden primer + each marker-only `[Plan cancelled]`), that Edit targets the message ITSELF and the tip is a legal target, and — the load-bearing canary — that `execute` **DISCARDS** its target. If the CLI ever flips to keep-semantics this FAILs loudly, because silently it would make Rewind/Edit eat one extra turn and revert the wrong files), **per-turn billing** (`usage-per-turn` — asserts every turn carries its own `_meta.usage` and that turn 2 is NOT cumulative; if the CLI ever switched to a running total, summing our log would silently over-bill and rewind could not subtract a discarded turn, both invisible in the UI), and the v1.4.x generative features. It **reuses the real compiled modules** (`out/acp-dispatch.js`, `out/plan-gate.js`, `out/grok-primer.js`, `out/rewind.js`, `media/webview-helpers.js`) — genuine wire output through the same `isMediaGenToolCall`/`extractGeneratedMediaPaths`/`isSubagentToolCall`/`shouldBlockWrite`/`GROK_PRIMER` the extension uses. **The `release.*` scripts now run it by default** (mandatory gate; `-SkipLive`/`--skip-live` is the explicit escape hatch), and you should still run it by hand before a release. It needs a logged-in grok + subscription and burns credits, so it's never in `npm test` or CI. Flags: `--smoke` (fastest lane — handshake + capability-drift only, no prompt turns), `--quick` (skip the slow tests incl. the now-4-turn plan-mode), `--only=`, `--skip=`, `GROK_BIN=…`. A SKIP does not fail the gate — only a FAIL does. Real-grok **diagnostic probes** (`research/*.cjs`) remain manual one-offs; the live suite is the repeatable gate.

**So:** local `npm test` == CI's `test` job (both grok-free) — but that is NOT all of CI: a second required job runs the grok-free Electron smoke, so `npm test` alone is a subset of what must pass. Mirror it with `npm run test:integration`, which `release.*` now gates on too. The real-grok tests are a separate, mandatory pre-release gate — always run before a tag/release (no need to ask), never on every commit.

### grok CLI version + updating

The native-Windows build (`irm https://x.ai/cli/install.ps1 | iex`) installs from the **stable** channel. The extension pins Windows to the supported build (`GROK_STDIO_DOWNGRADE_TARGET` in [src/cli-locator.ts](src/cli-locator.ts); see § Status → Grok CLI compatibility). **grok does not auto-update.** Updating is the explicit `grok update` command: `--check [--json]` checks without installing, bare `grok update` installs the latest on the current channel, `--stable` (default, weekly) / `--alpha` switch channels, `--version <X.Y.Z>` pins a build. `grok update` can only rename the binary when no grok process holds it open (else *"cannot rename locked executable"*), so the extension's update path tears the whole pool down and **awaits each process's actual exit** before updating; on Windows it `taskkill /T /F`-kills the process **tree** (grok backgrounds children that keep the binary locked) and retries once on a lingering lock (`isLockedBinaryError`). Re-run `npm run test:live` after any CLI update — the wire format is the thing that drifts.

## Install

- **macOS / Linux / WSL Ubuntu:** `./scripts/install.sh`
- **Windows (native):** `pwsh scripts\install.ps1` — runs the native Windows `grok` CLI directly
- **WSL Ubuntu (alternative):** Remote-WSL → install in the WSL-side VS Code server

See `README.md § Install` for the full per-platform matrix.

## ACP surfaces implemented

- `initialize` → `session/new` / `session/load` → `session/set_model` → `session/prompt` lifecycle
- **Model switching is agent-aware.** Models belong to *agent types* — `grok-build`/`grok-build-plan` vs the `cursor` agent that owns the Composer models. The CLI binds the agent at spawn and locks it after the first turn (incl. our primer), so a live `session/set_model` only works within the same agent; a cross-agent switch errors `MODEL_SWITCH_INCOMPATIBLE_AGENT` ("Start a new session"). `switchModel` in `sidebar.ts` tries the live switch, and on that error (detected by the pure `isIncompatibleAgentError` in `acp-dispatch.ts`) persists the pick to `grok.defaultModel` and restarts — `newSession` re-applies the model *before* the primer runs, while the agent is still rebindable. No history → transparent restart; with history → the same Summarize/Just-Restart prompt as an effort change (shared `pickRestartMode`/`restartSession` helpers). **Empty-session cleanup:** a restart on a *primer-only* session (no real conversation — the common case when you flip models/effort a few times right after opening) gets the no-prompt transparent path *and* `discardRestartedEmptySession` deletes the abandoned grok session dir afterward, so repeated switches don't pile up identical primer-only sessions in history; the pure `carrySessionName` (in `sessions.ts`) moves any user rename (`customName`) from the discarded id onto the fresh one so the chosen name survives. The same cleanup runs on the effort-change empty-session branch (guarded so a dead client on a session *with* history keeps its history). The toolbar model label resolves IDs to user-facing names via the pure `modelDisplayName` helper. **Versioned-id handling:** grok's `set_model` echoes a *versioned* id (`grok-build` → `grok-build-0.1`) that isn't in `availableModels` and carries no name/context of its own, so `setModel` anchors to the *requested* list id (always resolvable to a name + window); the pure `resolveModelId` in `acp-dispatch.ts` (exact → longest base-id prefix → input) is the fallback for grok-provided ids on `newSession`/`loadSession`. Context window is per-model (`_meta.totalContextTokens`: grok-build 512K, Composer 200K), recomputed in `chat.js` on every `modelChanged`.
- Streaming `agent_message_chunk` + `agent_thought_chunk`
- Sessions: list/resume via `session/load` (grok stores them at `~/.grok/sessions/<urlencoded-cwd>/<id>/`); rename/delete metadata in `context.globalState["grok.sessionMeta"]`. We never edit grok's own session files. **Per-session delete** is the trash button on each non-active history row (`deleteSession` → `deleteSessionDir`); the active session has no delete (the live CLI re-persists it, so it wouldn't stick). **Clear all** (#19) is a footer button in the history popover (shown only when ≥1 non-active session exists) → `clearAllSessions` in `sidebar.ts`: a confirmed sweep of every session dir for the *selected repo* *except* the focused one, via the pure `clearSessions({fs, grokHome, cwd, exceptId})` in `sessions.ts` (best-effort — a locked dir is skipped, not thrown), then purges the removed ids' `grok.sessionMeta` overrides and tears down any backgrounded live pool members it deleted. **Scale (history pagination):** the popover loads **one page at a time** (newest-first by last activity) so it stays fast with thousands of sessions — see § History pagination. Clear-all is the relief valve for an overgrown store; the pagination is the steady-state fix.
- Handlers (mandatory or the agent crashes): `fs/read_text_file`, `fs/write_text_file`, `terminal/{create,output,wait_for_exit,kill,release}`
- `session/request_permission` → chat card with `allow-always` / `allow-once` / `reject-once`, diff editor preview for `kind:"edit"`. **Keyboard (#68):** the CLI's option order isn't guaranteed, so `orderPermissionOptions` (webview-helpers.js) sorts approve-first/reject-last (unknown kinds rank between — never the default, never below reject) and `defaultPermissionIndex` picks the tab stop: `allow_once` only, **never `allow_always`** (a keystroke must not widen scope for the session). The card takes focus only when `shouldFocusPermissionCard` says the composer is genuinely idle — empty, not replaying, no IME composition in flight (`state.composingIME`, since a preedit buffer isn't in `input.value`). Roving tabindex + arrows move within the group; Escape returns to the composer **without answering** (never an implicit reject); and `isTypeThroughKey` redirects a printable keystroke to the composer instead of activating a button, which is what makes taking focus free. The reporter's "wait 1s after typing" timer is deliberately **not** implemented — the action a key takes must follow visible focus, never invisible timing, least of all when that action approves a command.
- `x.ai/ask_user_question` (#12) → inline question card. Renders each question's options (single question + single-select resolves on one click; otherwise pick-then-Submit; Skip → cancel) and replies `{ outcome: "accepted", answers, annotations }` (or `cancelled`) — `answers` keyed by question text → chosen label; the reply **must** carry `outcome` (an empty ACK is rejected). On answer the card collapses to the question + a green `✓ <choice>`. On resume the question replays as a `tool_call` (questions in `rawInput`) + completed `tool_call_update` (answer text); chat.js suppresses the generic tool chip and rebuilds a read-only "You answered" card from that replay. Pure builders: `makeQuestionResponse` (`acp-dispatch.ts`), `buildQuestionAnswers` (`webview-helpers.js`). Wire format: `research/ask-user-question.md`.
- `session/set_mode` wired; the picker exposes **Agent**, **Plan**, and **Auto accept** (the auto-approve mode — internally still the `"yolo"` mode id / `autoApprove` flag; "YOLO" survives only in the picker description). The CLI's non-plan mode id is `"default"` (not `"agent"`), captured as `ACT_MODE_ID` in `sidebar.ts`. **Config-forced auto-approve (#31):** grok's global `permission_mode = "always-approve"` (config.toml, set via Shift+Tab or `/always-approve`) auto-approves every session server-side and is invisible over ACP, so `newSession` reads the config (`configForcesAlwaysApprove` in `grok-config.ts`) and starts the session in "Auto accept" with a one-time notice, instead of a misleading "Agent"; the extension can't disable a global config setting per-session. **Mid-turn switching (#64):** the mode picker stays live during a running turn (webview gate is `busyLocked`, the startup window, not `busy`) — the host `setMode` already tolerated mid-turn calls (client-side `autoApprove` gate; `client.setMode` best-effort), so flipping to Auto accept takes effect immediately AND `autoApprovePendingPermissions` resolves any card already on screen (not just future requests). Approving a plan restores the **pre-plan** mode (`grok.defaultMode`, the last non-plan choice) in `handleExitPlan`'s approved `afterTurn` — Auto accept stays Auto accept through implementation instead of dropping to Agent.
- **Plan mode is enforced client-side** (mirror of YOLO). The CLI's `x.ai/exit_plan_mode` still treats any client response — result *or* error — as approval (re-verified broken in 0.2.3), so we don't rely on it. Instead `src/plan-gate.ts` gates the two *mandatory* server→client choke points: `fs/write_text_file` (block writes resolving inside the workspace cwd) and `terminal/create` (block anything not on the read-only allowlist). grok's own `~/.grok/sessions/<…>/plan.md` write lands *outside* the workspace and is allowed (and snooped to recover the plan text — `exit_plan_mode` arrives with `planContent: null`). Approve → drop the gate + send an "implement it now" follow-up prompt; Keep planning → gate stays up; a comment-less Cancel is silent — the notice is the whole UX, the `[Plan cancelled]` follow-up runs as a hidden turn (no dots, no ack bubble), and turn end always clears the dots indicator (an empty ack used to orphan it). Every verdict also cancels + content-suppresses the remainder of the planning turn (`suppressPlanReject`) — responding to `exit_plan_mode` unblocks it server-side and grok's primer-trained continuation is contentless "I'll wait for your verdict" filler that grok doesn't persist to history, so it must not paint live either (Approve skips the reject path's `agentReset` so pre-card narration stays on screen). A verdict also buffers a `planResolved` event (the plan twin of `permissionResolved`) so a warm re-focus replays the card collapsed instead of resurrecting the buttons. Resolved cards (live, replayed, or disk-restored) show just the plan-file link + verdict label — no inline plan text; the Show/Hide toggle survives only when no plan file exists (snapshot creation failed / legacy). Entering plan mode *any* way (incl. agent-initiated `current_mode_update: plan`) raises the gate; it's lowered only by explicit user action, never auto-lowered by CLI mode flapping. The verdict protocol is taught by a hidden **primer** (`src/grok-primer.ts`) — it tells grok to ignore the bogus `exit_plan_mode` result and read `[Plan approved]`/`[Plan rejected]`/`[Plan cancelled]` (optionally + a comment) from the follow-up message. **The primer fires eagerly + non-blocking** as its own hidden turn the moment a session goes live — on new AND restored sessions, and after `/compact` — *not* in front of the user's first prompt and *not* at composer-disabling cost (`ensurePrimed` in `sidebar.ts`, kicked off from `startSession`'s success path). It returns a reused `session.primingPromise` (`src/session.ts`) so a racing first send awaits the *same* in-flight primer (grok runs one turn at a time) and is released the instant the silent primer acks; the user can type and send straight away — their bubble shows immediately (a "Grokking…" indicator stands in until grok produces content) while the real prompt is held behind the primer in code. On restore the primer is *re-sent* on first go-live rather than trusted from replayed history (a `/compact` can drop it). The primer's silent turn is hidden by a session-level `suppressContent` flag checked in `post`/`emit` against `SUPPRESS_TYPES` (which deliberately **excludes** `userMessage`/`agentStart` so a user send that races the background primer still paints its own bubble + Grokking indicator). The primer (`PRIMER_VERSION`/`PRIMER_MARKER`) is deliberately minimal — the plan-verdict protocol plus an explicit *do not use any tools / do not read files / do not search the workspace / do not take any action* constraint ending in "Reply with exactly: ok" (grok-build is agentic; anything beyond the protocol tempts a slow pre-turn workspace exploration). The pure `isPrimerText()` (version-agnostic `PRIMER_PATTERN`) detects the primer when grok replays it so the bubble is hidden + not counted toward plan positions. Full course: `research/understanding-plan-mode.md`.
- `grok.defaultEffort` → forwarded as `--reasoning-effort <value>` **before** the `stdio` subcommand (it's an agent-level flag; after `stdio` the CLI errors "unexpected argument"). Offered values are grok's accepted set `none|minimal|low|medium|high|xhigh` (`max` is rejected with exit code 2, so it's not offered — #3/#4). Args are built by the pure `buildGrokAgentArgs()`. **Changing effort applies LIVE when the CLI supports it** — the current model advertising `models[]._meta.supportsReasoningEffort` (grok 0.2.101+) gates `client.setReasoningEffort` → `session/set_model` with `_meta.reasoningEffort`, so `setEffort` (`sidebar.ts`) changes effort on the running session with no restart. Falls back to the restart path (`pickRestartMode`/`restartSession`) when the CLI lacks the capability, when the live switch is rejected, or when unsetting back to the model default (`""` can't be expressed as an override).
- `grok.chatFontScale` (#14, webview-only — **not** ACP) → local VS Code chat-panel zoom as a percent (default 100, clamped 60–300). The host bakes the initial value into `<body style="zoom:…">` in `getHtml` (no flash) and `postFontScale` re-posts a `fontScale` message on `onDidChangeConfiguration` for a live, reload-free update. `applyChatZoom` uses that host value only in the local webview; AFK Pilot uses only its browser-local `grok.remote.fontScale` slider (80–160%), so desktop zoom changes never multiply remote zoom. `zoom` scales the whole chat — text, icons, spacing — but not the rest of VS Code (the point vs. global `Ctrl/Cmd+Shift+=`). Supports User/Workspace scope for free. `chatFontScale()` in `sidebar.ts` reads + clamps. The **scroll-to-bottom button** (#28) lives inside `.composer` (`position:absolute`), so it rides this same zoom scale instead of fixed viewport pixels.
- `grok.showThinking` (#26, webview-only — **not** ACP) → whether grok's reasoning traces are shown; **default false**. The host bakes a `thinking-hidden` body class into `getHtml` (no flash) + posts `showThinking` on init and on `onDidChangeConfiguration`; chat.js's `applyThinkingVisibility` toggles the class (`body.thinking-hidden .msg.thinking { display:none }` — works on replayed/old sessions, instant flip). When hidden, the pure-CSS `loading-dots` **"Thinking…"** stand-in (brain icon) shows while grok reasons. A **continuous-indicator guarantee** (`ensureActivityIndicator`/`turnHasVisibleActivity`, called after each `TURN_PROGRESS_MSGS` event) keeps **Grokking / a running tool / Thinking…** on screen for the whole live turn — never a dead frame (skips the locked priming window + replay). Toggle in gear → **Config & debug** (a `.popover-switch`).
- `grok.soundNotifications` (#59, webview-only — **not** ACP) → an optional short tone on turn completion/error, **default false**. Synth tones via Web Audio in `chat.js` (`playNotificationTone` — rising two-note chime for `done`, lower falling tone for `error`; no bundled assets, CSP-safe, offline); the `AudioContext` is created lazily and `unlockAudio`-resumed on the first user gesture (autoplay policy). `maybeNotifySound` gates on the setting **and** the panel being unfocused (`document.visibilityState === "hidden" || !document.hasFocus()`) — the "notify me when I've stepped away" case, not a beep on every turn you're watching. The `agentEnd`/`agentError` handlers additionally skip replayed events, so reconnect snapshot history cannot notify. `agentEnd` is in `SUPPRESS_TYPES`, so the hidden priming turn can't beep. Wired the `steerByDefault` way (host posts `soundNotifications` on init + `onDidChangeConfiguration`; gear `.popover-switch` posts `setSoundNotifications`).
- `grok.readRepliesAloud` (webview-only — **not** ACP) → local VS Code speech synthesis for completed replies, **default false** and independent from AFK Pilot's browser-local `grok.remote.tts` / `window.grokRemoteTts` preference (also **default false**). Both surfaces render the switch in gear → Config & debug; local posts `setReadRepliesAloud` for host persistence, while remote writes only localStorage and keeps the hook live. `speakCompletedTurn` runs on `agentEnd`, uses `spokenTextFromMarkdown` to omit fenced code, and selects the surface-specific flag. `buildRemoteSnapshot` wraps the full buffered transcript in nest-safe `historyReplay` brackets, suppressing TTS for already-completed replies on reconnect; if the buffered turn is still running, replay end seeds its rendered prefix so a later live completion speaks the whole reply exactly once.
- `grok.defaultMode` (#25, webview-only — **not** ACP) → the remembered last non-plan mode (`"" | "agent" | "yolo"`), applied to **new** sessions only (resumed ones keep their verdict-driven plan/agent state). `setMode` persists it on every user switch except Plan (a transient per-task choice); the new-session path flips `autoApprove` when it's `"yolo"`. Mirrors how `defaultModel`/`defaultEffort` already persist.
- **Telemetry** (`grok.telemetry.enabled`, default **true** — **not** ACP). One anonymous Aptabase `session_start` event per session, fired from the real-send path at `isFirstSend` (`!session.hasHistory`), so the primer (separate `client.prompt` path, never sets `hasHistory`) and empty/abandoned sessions never report. Props = an anonymous install GUID (`grok.installId` in globalState, survives updates) + `mode`/`model`/`effort`; local UI configuration (`showThinking`/`expandToolDetails`/`steerByDefault`/`chatFontScale`/`readRepliesAloud`/`soundNotifications`); browser-reported `remoteFontScale`/`remoteReadRepliesAloud` when an AFK Pilot client is present; first-message `sessionOrigin` (`local`/`remote`) + `clientDevice` (`desktop`/`mobile`, with remote mobile inferred from the browser's coarse-pointer/no-hover signal and local always desktop); and the host app (`vscode.env.appName`: VS Code/Cursor/Antigravity — omitted, never blank, when unreported); **no content**. AFK Pilot posts the typed `remotePreferences` message (including `usesTouch`) at boot, on preference changes, and on each `session` message only after an `initialState.readRepliesAloud` field proves the host supports it; the remote policy allows it as harmless view metadata and the host stores it on `Session` until the first-send event is built. The dispatcher threads its `origin` into `handleSend`/`reportSessionStart`, so the surface is attributed to the client that actually sent the first message. All props are disclosed in [docs/privacy.md](docs/privacy.md). Triple-gated via the pure `shouldSendTelemetry(vscode.env.isTelemetryEnabled, grok.telemetry.enabled, isOfficialBuild)` — any one off silences it; `isOfficialBuild` is `context.extension.id === OFFICIAL_EXTENSION_ID`, so a fork republished under another publisher id never reports (the Aptabase app key is a write-only client key that ships in the vsix, so this id gate — not key secrecy — is what keeps forks' usage out of the project). Region host comes from the app-key prefix (`aptabaseHost`); `postEvent` is fire-and-forget over `node:https`. Country is Aptabase-derived from IP server-side (IP then discarded). Pure helpers in `src/telemetry.ts`; no SDK dependency. **Two Aptabase projects:** the **official** build reports to `APTABASE_APP_KEY_PROD` (dev host, local vsix installs of the official id, and the Marketplace build alike — `isDebug` flags the dev host; a fork under a different publisher id reports nowhere); the `APTABASE_APP_KEY_DEV` project receives **only** the manual `npm run telemetry:probe` (`scripts/telemetry-probe.cjs`), which fires real `session_start`s through the *compiled* `out/telemetry.js` so test traffic never pollutes prod. The `npm test` unit suite stays network-free — it asserts the payload/builders, never POSTs.
- `available_commands_update` → slash autocomplete + the `matchSlashCommand` dispatch gate (a typed `/command` must stay at position 0 of the prompt's text block or the CLI won't dispatch it — the confirmed-command send flips the context envelope behind the text; a confirmed `/compact` also re-primes afterward, since the CLI's history rewrite can fold the hidden primer away, and the compact turn's response reports `totalTokens: 0`, which `gateZeroTokenMeta` strips — the fresh post-compact count comes from the `auto_compact_completed` notification rail, not this turn's meta (see the `_meta.totalTokens` bullet). Wire evidence: `research/compact.md`, `research/grok-build-oss-findings.md`)
- `current_mode_update` → bottom-toolbar mode button
- `_meta.totalTokens` → context donut. The host strips a `totalTokens: 0` report before it reaches the webview (`gateZeroTokenMeta` in `acp-dispatch.ts`) — grok reports 0 for `/session-info` (context untouched) and `/compact` (context shrunk, not emptied), so 0 is never a real measurement (#39); the donut keeps its last real value and the "Compacted." bubble is the compact worked-signal. The real count for the cases the meta can't cover rides the `contextUsage` host message, from two sources: **(a) cold restore** — the pure `readContextUsage` (`sessions.ts`) reads the session's persisted `signals.json` (`contextTokensUsed`/`contextWindowTokens`), else the donut sits at 0 until the first turn; **(b) post-/compact** — the CLI recomputes `signals.json` only when the NEXT inference turn ends, never at the compact turn's own end (`research/signals-refresh-probe.cjs`). **Primary:** the fresh count rides the live `_x.ai/session_notification` rail — `auto_compact_completed.tokens_after` (the pure `contextUsedFromCompactNotification` in `acp-dispatch.ts`, handled in the `xaiNotification` listener), which fires for BOTH a manual `/compact` and automatic (context-full) compaction and sets `Session.sawCompactNotification`. **Fallback (pre-rail CLIs, e.g. the Windows downgrade target 0.2.72):** when that notification didn't arrive, `refreshContextAfterCompact` (`sidebar.ts`) runs a hidden CLI-local `/session-info` turn and `parseSessionInfoContext` (`acp-dispatch.ts`) pulls the exact count from its reply ("**Context:** N / M tokens"). The post-compact re-prime's `signals.json` read is the second backup — gated on `!sawCompactNotification && session.primed` so a failed primer can't clobber the good value with the stale pre-compact count. Clicking the donut opens the **context popover** — the exact `used / window (%)` line, the billing breakdown (#53, below — **Session total** leads because it's the number you act on; **Last turn** is a collapsed disclosure below it, its open/closed state remembered in the webview's own `getState`/`setState`), and **Compact conversation** (moved here from the gear menu: it is a *context* action, so it lives next to the number that motivates it; disabled at 0 tokens. The gear's Session section now holds *Fork*, keeping gear = session-lifecycle ops and donut = context ops). **Automatic (context-full) compaction** — silent before — now shows a dedicated one-line **notice** (`autoCompactNotice` host msg; the pure `autoCompactStartedNote` keys it on `auto_compact_started`, which the CLI emits ONLY on the auto path, never a manual `/compact`); `auto_compact_failed` renders a failure notice, and the manual `/compact` "Compacted." bubble is **gated on `Session.sawCompactFailed`** so it doesn't paint a false success. The notice is a dedicated message (not a `messageChunk`) so it finalizes any active bubble and can't reorder the agent's answer
- **Steer — mid-turn injection (#52).** `_x.ai/interject` (`{sessionId, text}` → `{status:"queued"}`) queues into the session's pending-interjection buffer, drained at the agent's next safe point. It **does not cancel** the turn and loses no in-flight tool work — that is the whole difference from Stop-and-resend, and it's pinned by the `interject` live test (the model must obey mid-stream *and* the turn must not end cancelled). **`grok.steerByDefault`** (webview-only, default **false**; gear → Config & debug → *Steer by default*, hidden when the CLI can't interject) is the policy: off = today's queue (with an on-demand Steer button), on = a send-while-busy **skips the queue** entirely. It branches in `queueOutgoing` (chat.js) — the single choke point both typed sends and dictated utterances pass through — and falls back to queueing on all three nothing-to-steer-into cases (`busyLocked` priming, `!steerSupported`, not busy). A queued block gains a **Steer** button while `body.turn-busy`; clicking it `dequeueSend`s + posts `steerSend`, and `steerSend` (`sidebar.ts`) paints the bubble optimistically then interjects. Steering carries **plain text only** — it bypasses `prompt-builder`, so no context envelope, no chips, no `/command` dispatch. On -32601 the host emits `steerUnavailable` (latches the button off), **re-queues the text**, and warns — a capability gap must never eat a message.
- **Fork — branch the conversation (#48).** Gear → Session → *Fork conversation* → `forkSession` → `_x.ai/session/fork`. Params are `ForkSessionRequest`'s three **required** camelCase fields (`sourceSessionId`/`sourceCwd`/`newCwd`; `{sessionId}` alone is -32602); the response's `newSessionId` is handed to `openSession`, so the fork loads like any history row. It copies session files under `grok_home()` **only** — the workspace is untouched, so **code is not rewound** (`/rewind` + `rewind_points.jsonl` is grok's separate file-snapshot feature). The parent is verifiably unchanged. Forks carry `session_kind: "fork"`, and `buildEntry` (`sessions.ts`) only special-cases `"subagent"`, so they appear in history with no client change. **Naming:** the fork is stamped with the parent's history name behind a **leading** `(Fork)` tag via the pure, **idempotent** `forkDisplayName` (`sessions.ts`) — leading because history rows ellipsize at the panel edge, so a trailing tag is the first thing to vanish; forking a fork stays `(Fork) Foo`, never stacks. The parent name comes from `sessionDisplayName` (`sidebar.ts`), which mirrors the list's precedence (`customName` → `session_summary` → first message) **except that it rejects a primer-derived summary** (`isPrimerSummary`): we prime every session, so grok titles it from the hidden primer and `session_summary` is routinely "… Primer v4 Plan Mode …" — inheriting that would propagate an internal name for an invisible message through every fork-of-a-fork. The parent name is resolved **before** the fork RPC (a turn landing mid-fork rewrites `summary.json` and grok's generated title with it) and written as the fork's `customName` **before** `openSession`, so no surface flashes grok's own title; the toast then points the user at the untouched original in history. **Whole-session only, deliberately:** `targetPromptIndex` works on the wire but truncates `chat_history` *without* truncating `updates.jsonl`, so a fork-at-message-N would replay a conversation the model has forgotten — blocked upstream (`research/grok-build-oss-findings.md` § 3a).
- **Worktree sessions (P2-8).** Gear → *New worktree session* (or `Grok: New Worktree Session`) creates an isolated git worktree via the **unadvertised** `_x.ai/git/worktree/create` (`sessionId` + `sourcePath`, optional `label`) under `~/.grok/worktrees/`, then starts a fresh session whose process **cwd is the worktree path** — so agent edits don't touch the main checkout until merged. *Apply worktree* merges edits back (`_x.ai/git/worktree/apply` with `sessionId` + `worktreePath`); *Remove worktree* disposes processes on that path then `_x.ai/git/worktree/remove`. All four RPCs return `"unsupported"` on -32601 so an older CLI degrades. History scans the workspace cwd **and** known worktree paths (pure `mergeSessionIndexes` / `collectSessionCwds` in `worktree.ts`), stamps a `WT <label>` badge (`worktreeLabel`) on rows, and reopens with the correct cwd (`resumeSession` now carries `cwd`). Meta (`SessionMetaOverride.worktreePath`/`worktreeLabel`/`sourceGitRoot`) survives reload. Probe-confirmed against grok 0.2.111; pure parsers in `src/worktree.ts`, wire notes in `research/worktree.md`.
- **Rewind (P2-9).** Hover a user bubble → *Rewind* (or `Grok: Rewind Conversation`) → `_x.ai/rewind/points` lists restore targets and `_x.ai/rewind/execute` rolls the conversation back — truncating chat **and restoring files on disk** from grok's `rewind_points.jsonl` snapshots (the CLI's own file-snapshot feature; a chat action reverting code is confirmed via a QuickPick first). Both RPCs return `"unsupported"` on -32601. Pure `parseRewindPoints`/`parseRewindExecute` in `src/rewind.ts`; wire notes in `research/rewind.md`. **Rewinding to N DISCARDS N and its response** along with everything after (probe-verified, `research/rewind-semantics-probe.cjs` — the name reads like the opposite, and reading it that way cost an extra turn per edit; the tip IS a legal target).
- **Edit and resend (#56).** Hover the **latest** user bubble → *Edit* (`editLastMessage`) → rewinds past it and posts `restoreComposer` with the bubble's text. Exact complement of Rewind, which hides on the tip (not a valid target), so between them every user message has a way back. **The bubble→point map only holds if every user bubble is a prompt** — a steered message (#52) is NOT: it rides inside the running turn (`synthetic_reason: "interjection"`) and gets **no rewind point**, so counting it shifted every later bubble by one, pointing Rewind at the wrong turn (reverting the wrong files) and making Edit fail. Steer bubbles are marked `data-steer` — live from `steer:true` on `userMessage`, on restore by the pure `isInterjectionText` matching the CLI's replay envelope — and `refreshUserRewindButtons` gives them no index and hides both actions. Because rewind DISCARDS its target, the wire target is the bubble's **own** point (`resolveEditRewindTarget`), so exactly one turn goes; editing the first user message discards its point and leaves the primer + its `ok` intact. It returns null only when the bubble maps to no point at all (rather than guessing a neighbouring turn), where the host offers copy-to-composer instead. The restored text is the **webview's** bubble text, never the execute result's `promptText`, which describes the KEPT message one turn earlier. `restoreComposer` **appends** to whatever is already typed rather than overwriting it. **Neither path reloads the session:** `applyRewindToView` emits `truncateMessages` (webview deletes the discarded tail in place) and cuts `Session.buffer` to the same point via the pure `truncateReplayBuffer` — a focus-swap replays that buffer, so leaving it whole resurrected every discarded turn. The old `clearMessages` + `startSession` blanked the panel to the welcome logo and re-rendered the whole conversation for a tail deletion. **Both rewind paths must also truncate OUR OWN cards:** persisted `plans`/`permissions` (globalState, positioned by `afterUserMessage`) are the extension's records — the CLI replays neither on `session/load` — so `truncateResolvedAfter` (`plan-restore.ts`) drops entries past `survivingUserMessagesAfterRewind` (`rewind.ts`, counted over user-FACING points so the primer/verdict plumbing can't inflate it) and `lastPlanVerdict` is recomputed from the survivors (it gates plan-mode restore). Subagent/media/command rows need no equivalent — they ride grok's own replay and truncate with it.
- **Run progress (P2-10).** Deep Research / Workflow / Goal turns stream progress on the live `_x.ai/session_notification` rail; the pure `isRunProgressUpdate`/`parseRunProgressUpdate` (`run-progress.ts`) normalize `workflow_updated`/`goal_updated` into a teal progress card with Pause/Resume/Stop (`workflowControlCommand` maps a control to the CLI's slash). See `research/run-progress.md`.
- **`_meta.usage` → billing breakdown (#53).** Nested inside the prompt result's `_meta` (and mirrored on the rail's `turn_completed.usage`), extracted by the pure `extractPromptUsage`. **Three quantities, never conflated:** `usage.*` is the WHOLE prompt's billing (across `modelCalls`); the flat `inputTokens`/`outputTokens` siblings are the LAST model call only; `totalTokens` is CONTEXT size (one probed turn: 16371 context vs 32488 billed). They don't decompose into each other, so the donut arc stays context-only and the popover shows usage as its own sections. **No cache-*creation* field exists anywhere in the CLI** — it is omitted, never faked as 0. `addUsage` sums the session total (absent + absent stays absent), gated by `usageIsRealMeasurement`: a `/compact` turn replays the previous turn's siblings verbatim, so counting it would double-bill. The popover shows **Model calls**, without which billed input (which routinely dwarfs context, since every model call re-sends the conversation) reads as a bug. The total is **ours** — grok reports per-prompt only and `signals.json` persists just context size — so each turn is logged to `SessionMetaOverride.usageLog` (positioned by `afterUserMessage`, like plans/permissions) and `usage` is the DERIVED sum re-seeded by `restoreUsage` on load. Per-turn entries exist so a rewind can **subtract** the discarded turns (`sumUsage` over the truncated log) — a running total alone cannot be undone; a session logged before `usageLog` existed keeps its stored total uncorrected rather than dropping to 0; a session with no stored total shows context only.
- **Vision input (image paste/upload).** Ctrl+V in the composer, drag-drop, and the + picker attach raster images (png/jpe?g/gif/webp — SVG deliberately stays a *path* chip so grok can read/edit the source; 20MiB cap per the xAI docs) as inline `{type:"image", mimeType, data:<base64>}` content blocks in `session/prompt`, plus a `[Image #N]` tag line in the text block (`envelope\n\ntext\n\ntags` — text keeps position 0 so `/commands` still dispatch; disk imports carry the origin path in the tag, `[Image #1] (assets/x.png)`, so grok can act on the real file). **The CLI still advertises `promptCapabilities.image:false` but actually accepts the blocks** (verified 0.2.87; behavior over advertisement — no capability gate, the `vision-prompt` live test pins it instead; see `research/vision-input.md` + `research/vision-probe.cjs`). Pending images live in a session-NEUTRAL staging dir (`<globalStorage>/image-staging/` — a grok session dir gets deleted by the empty-session cleanup, which would kill a pending screenshot), are pre-read + validated at send (any unreadable image **blocks the send** with chips intact — a dangling tag sends the model hunting for an image it never got), and are unlinked after send / chip removal, with an age-gated sweep on activation for orphans. `[Image #N]` numbering is session-scoped (`Session.imageCounter`, re-seeded from replayed prompts on restore). The webview holds send while a paste's FileReader is in flight (`pendingPaste`) and keeps the text half of a mixed clipboard; chips are host-owned — `send` carries only the text.
- **Remote document upload.** `initialState.capabilities.uploadFile === true` is the additive host capability proof; an updated web portal may then send `{type:"uploadFile",name,data}` with base64 bytes. The host re-validates the sanitized basename, six-extension allowlist, and shared 20MiB cap, stages to `<globalStorage>/file-staging/<uuid>/<basename>`, and routes the path through `addDroppedFile`, producing the same explicit path chip and `Attached file:` prompt as local drag-and-drop. Sent documents are retained in `SessionMetaOverride.uploadedFiles` until the last referencing session/fork is deleted because Grok reads the path lazily; only unreferenced orphan directories are swept after seven days.
- **Command details (#41).** The extension runs every shell command itself (the mandatory `terminal/*` handlers), so each command row carries an always-available, collapsed-by-default expandable detail — a Claude-Code-style **IN/OUT block** on the shared code-chip surface: IN = the FULL command text from `rawInput.command` (grok truncates its titles), OUT = the complete captured output — each **capped to the first `MAX_COMMAND_PREVIEW_LINES` (6) logical lines, no inner scroll** (#71; pure `commandTextPreview`), the rest reached via **"View all"**: on the desktop it opens the full text in an editor tab (`openText` → `sidebar.ts` untitled doc), on a remote client (no host editor) it **expands inline** (`IS_REMOTE`, the same audience-split the diagram export uses) — a single long logical line shows in full. Snapshotted at `terminal/release` in `acp.ts` (`commandDone` → `commandOutput` host message; the buffer is exactly what grok received — same byte cap — plus a 100K display cap in `sidebar.ts`). Every tool row is ONE line (`.tool-item-label` span, CSS-ellipsized at the row edge as backstop; `toolLabel`'s 40-char clamp is the deliberate scannable-summary trim — the full text lives in the detail; a COMMAND row instead reads "Run \<program\>" via the pure `commandProgramLabel` (webview-helpers.js): first token, path-stripped/de-quoted, + one non-flag **bare** subcommand (a *quoted* next token, or one containing a path separator, is an argument/banner not a subcommand, so it's dropped — `Write-Output '=== 1. git status ==='` → "Write-Output"; `node research/x.cjs` → "node"), first-statement-only, "command" fallback. A **`(cd dir ; cmd)` subshell** (grok's POSIX navigate-then-run idiom, emitted even against PowerShell) is unwrapped — the `( )` stripped and a leading `cd <dir>` prelude skipped — so it labels the real command (`(cd … ; node x)` → "node"), not "(cd"; the cd-skip is scoped to subshells, so a user-typed `cd src && npm test` still reads "cd src" — so `git status`/`npm test`/`Get-Date` stay scannable while a `node -e "…"` payload collapses to `node`); a lone RUNNING command's group is expandable immediately (`cmd-single` — header chevron shows mid-run and expanding auto-opens the row's IN/OUT; multi-tool groups keep their chevron until the batch closes); expandable command rows carry a TRAILING chevron (› collapsed ↔ v expanded, `expanded` class) + hover brightening (`has-details`, `wireCommandToggle` — plain rows like the output poller don't react), and clicks inside the block never collapse it (text selection). Exit codes merge into the output: exit 0 with output is silent, exit 0 with **no** output renders a muted `✓ done · no output` marker (no empty `(no output)` pre), non-zero renders an `[Error] exit N` marker + error-tinted output **and rolls the failure up to the row (`.tool-failed`) + group (`.has-error`)** — a failed command signals at a glance like a `status:"failed"` tool (`markToolFailed`), not just inside its OUT box, a kill renders muted `[Cancelled]`. Output attaches by **two paths, one per agent execution model**: **grok-build** delegates commands via `terminal/create`, so the client snapshots the buffer at release and attaches by exact command string (`commandOutput` → `state.pendingCommandDetails`, `attachCommandOutput` in chat.js) with a standalone fallback row so output is never dropped; the **cursor/Composer** agent runs commands in its own CLI-side persistent shell (never `terminal/create`), so its output rides the **completed `tool_call_update.rawOutput`** and is attached by **`toolCallId`** (`maybeAttachToolResultOutput` + the pure `extractToolResultOutput` in webview-helpers.js — order-independent, because Composer runs commands in parallel and finishes them out of issue order, so a FIFO/order-based guess would misattribute). Both paths reference the same `details` node stored on the `pendingCommandDetails` entry (keyed by both `command` and `toolCallId`), which survives a lone command's flatten to `.tool-flat` (the flatten MOVES the chevron + details nodes). `grok.expandCommandOutputs` (webview-only, default false; gear → Config & debug → **Expand tool details** — label renamed to match the two Command-Palette commands, setting KEY unchanged; mirrors the showThinking pattern via `applyExpandCommandOutputs`) pre-opens every detail **and** every detail-bearing tool GROUP (a group is detail-bearing iff it has a `.has-details` row — a command's IN/OUT **or** an edit's diff; read/explore-only groups stay collapsed) — the group open state is applied both live (`applyExpandCommandOutputs`) and at group finalization (`closeToolGroup`) — except on a group the **user** has toggled (`el._userToggled`, set by the header click), which `closeToolGroup` leaves alone: expanding a *running* batch to watch it must not be undone the instant it finishes. An explicit setting change or Expand/Collapse-All latch flip still force-applies through `applyExpandCommandOutputs`, so a global action always wins. On-demand, the Command Palette `grok.expandAllToolDetails` / `grok.collapseAllToolDetails` (titled *…(This Session)*) → `sidebar.setAllToolDetails` **`emit`s** (not `post`s) a `setAllToolDetails` host message that flips a per-session **latch** — `state.toolExpandOverride` (tri-state `null | true | false`) in chat.js. The latch force-expands/collapses **every** group (incl. explore/edit) + IN/OUT box — including a still-running batch — and keeps applying to later tool calls, because all render points flow through the effective-expand helpers `groupShouldExpand`/`detailShouldExpand` (`override ?? persisted-setting`). It beats the persisted setting (last action wins; flipping the gear switch or the `expandCommandOutputs` message clears it), **rides the session replay buffer** (the `emit`, not a `post`) so it survives Agent-Dashboard focus-swaps, and resets on a cold reopen (`resetForNewSession`) — never persisted to disk. CSS shows a running group's chevron once it's `.expanded`. Failed non-shell tools surface their reason via `toolFailureText`, which mines variant-keyed `rawOutput` blobs (e.g. `list_dir` → `NotFound`, `read_file` → `FileReadError`) after `message`/`content`/`error`, else the generic "Tool call failed." Radius scale: bubble 12 → block code/IN-OUT 8 → inline chip 6 (`--chat-radius-*`). Live-session only: the CLI doesn't replay terminals on `session/load`.
- **Edit diffs inline (#45).** Every `kind:"edit"` tool row carries an always-visible `+N −M` change count (green/red, rolled up onto the collapsed group header as **path-deduped** totals, e.g. *Edited 2 files · +7 −2*) **plus** an expandable inline diff on the same detail surface commands use — so an edit is auditable in chat even under **Auto-accept**, where no permission card fires. This is decoupled from the permission flow on purpose: the diff `content` block (`type:"diff"`, with `oldText`/`newText` = the replaced region) rides the `tool_call_update` regardless of permission mode, and whether a permission card even appears is grok-build/platform-dependent, not a config toggle (verified 0.2.99: default config sends 0 permission requests for in-workspace edits; `research/edit-diff.md`). The pure `computeLineDiff(oldText,newText)` (`webview-helpers.js` — LCS backtrack, CRLF-normalized for compare+display, empty region = 0 lines so a new file reads as pure adds) yields the `+/−` counts and the diff rows. **A block is expanded into one hunk per replaced SITE** (`extractDiffSites` in chat.js): the block's `oldText`/`newText` is the search *pattern*, so a `replace_all` block describes only the token and alone renders 148 occurrences as one meaningless `+1 −1` — `_meta.details[]` is the complete account (one entry per site, `{old_string, old_line, new_string, new_line, context_before, context_after, line_prefix}`, real 1-based **post-edit** file lines; verified 12/12 against ground truth, `research/edit-diff-lines.log`). Per delivery shape: **completed + session/load** carry `details[]` → a site each, rendered `line_prefix + old_string` → `line_prefix + new_string` (there is **no `line_suffix`** on the wire, so the line's tail is genuinely unavailable and is not reconstructed); the **echo** carries no `details[]`, only a first-site-only block `_meta.{old_line,new_line}` → one approximate hunk the completed update then upgrades; neither (a whole-file Write's `_meta:{}`) → one hunk seeded 1 via `fileLineOr1` (also the guard for absent/bogus — 0/negative/non-number). A creation (`old_string:""`) takes no prefix, so a new file stays pure adds instead of the prefix becoming a phantom del. `buildInlineDiffRegion(hunks)` renders them **Codex-style**: **ONE `.tool-diff-region` per BLOCK holding N hunks** (never one region per site — 148 sites would otherwise stack 148 boxes) of `.tdl` rows, each a `[+/− sign][line-number gutter][code]` grid with a colored left-border stripe + subtle per-line tint; each hunk seeds its own gutter, and a quiet `.tdl-sep` hairline parts **non-contiguous** hunks (never before the first, never between contiguous ones). the region shows the first `DIFF_PREVIEW_LINES` (12) rows with a **"Show more"/"Show less"** toggle that grows it **inline, no inner scroll** (#71; remote-safe, unlike the host-local `open diff →`); `MAX_INLINE_DIFF_LINES`=400 is the hard budget **across** a block's hunks (1000 sites must not paint 2000 rows), the `open diff →` remainder note hidden until expanded — but the `+N −M` is summed over **every** site *before* the cap, so a capped render still reports the true magnitude. Del shows the old-side number, add/context the new-side. The gutter track sizes per region to the widest number rendered (`--tdl-num-w` = `max(4ch, digits+1)`) — 4ch through 999, wider only for a 1000+ line file. The `+/−` glyph is a color-blind affordance beside the border. Colors are **Codex's exact palette** (`--tdiff-*` CSS vars, dark default + `body.vscode-light` override): add `#40c977`/`#00a240`, del `#fa423e`/`#ba2623`, backgrounds the same at 23%/15% alpha — reused for the `+N −M` stat too. `applyToolDiffs`/`attachDiffPreviewToToolItem` handle BOTH the live `tool_call_update` and the session-load completed `tool_call` replay shapes (#30); a single call can carry multiple `diff` blocks (rendered as an array), each getting exactly **one** `open diff →` (per-site would emit 148 buttons; its payload stays the block's own `oldText`/`newText`). **grok reports each edit's diff twice** — an optimistic pre-write echo, then the authoritative completed update — and for a whole-file Write that OVERWRITES a file the echo carries `oldText:""` while the completed one carries the real prior content, so the echo must not win: idempotency is keyed on `item._diffSig` (the serialized diffs, **sites included**, so the `details[]`-seeded render outranks the echo's approximate one), making an identical replay a no-op while a *different* later diff repaints the row (reusing the detail node, or `wireCommandToggle`'s listener would strand + double-bind). **Totals paint as each edit lands**, not at batch close — the data is on the wire ~2.2–3.3s before the turn ends (`research/edit-diff-timing-probe.cjs`). `recomputeGroupDiffTotals` *recomputes* from the rows' `_diffStat` (so a corrected row replaces rather than double-counts) and `paintGroupDiffTotals` owns a `.tool-group-diff-totals` span, re-painted after each of the two things that wipe the header: `addToToolGroup`'s per-call `hdr.innerHTML` rebuild and `closeToolGroup`'s label reset. Painting earlier still, from the initial `tool_call`'s `rawInput`, is deliberately NOT done: it buys ~30ms and reconstructs the same wrong `oldText:""` for a Write. None of this expands anything — totals ride the *collapsed* header; only `grok.expandCommandOutputs` / the latch open rows. The native `open diff →` link (→ `sidebar.ts` diff editor) stays for the full side-by-side. `summarizeTools` counts edits by unique `toolFilePath` so "Edited N files" matches the deduped header totals.
- **Generated media (v1.4.x).** `/imagine` (`image_gen`, or `image_edit` for reference-photo edits) and `/imagine-video` (`video_gen`; older/Linux builds `image_to_video`) are subscription-only and do **not** return ACP image blocks — grok writes the file into its session dir (`images/*.jpg`, `videos/*.mp4`) and reports the path as a JSON string in the completed tool result's `text` content. The pure `isMediaGenToolCall`/`extractGeneratedMediaPaths` (in `acp-dispatch.ts`) detect the tool and parse the path (image-vs-video by extension); `acp.ts` tracks the tool-call id (the *completed* update has a null title) and emits `mediaContent`; `sidebar.ts` `postGeneratedMedia` serves the file via `webview.asWebviewUri` (streamed from disk under the grok-home `localResourceRoot`; a base64 `data:` URI is the fallback for files outside the served roots). CSP grants `img-src`/`media-src ${webview.cspSource} data:`. Inline media is capped at 320px with Copy-path / Open-in-VS-Code hover actions. On resume grok replays it as a single collapsed `tool_call` carrying title + path together. Wire format + probes: `research/image-generation.md`.
- **Subagent rows.** A delegation is a tool call whose `_meta["x.ai/tool"].name` is `spawn_subagent` (grok-build) or `Task` (the Composer agent) — that marker is **authoritative both ways** for the pure classifier (`isSubagentToolCall`, `webview-helpers.js`): grok titles Grep/Read calls with their query/filename, so a grep FOR "spawn_subagent" is titled exactly that and title matching false-cards; without `_meta`, exact tool names + `rawInput.subagent_type` + the legacy background heuristics apply (the `get_command_or_subagent_output` poller stays excluded). The row renders *Subagent · \<description\>* (never downgraded by Composer's untitled completion update — `setSubagentTitle`) with blink-dots, then a duration stamp and a click-to-expand result under an "Output of the subagent:" label; `cleanSubagentOutput` strips the CLI envelope (plumbing tags, lead-ins, one wrapping `<response>` pair, the Agent ID hint) — every pattern optional and never throwing. Completion shapes differ: grok-build's foreground `tool_call_update` carries `rawOutput.SubagentCompleted` (output + duration_ms); Composer's carries only untitled text (`rawOutput {type:"Text"}`, no duration); a **background** spawn "completes" instantly with a started-ack (skipped — not the result) and its real output arrives on the `get_command_or_subagent_output` poller's `TaskOutput`, matched back to the card by task id (`maybeFinishSubagentFromTaskOutput`). **On cold `session/load` that poller output is FLATTENED to a text blob** (`=== Task … === / Command: [subagent:…] / … / === Output ===`, not the live structured shape), so the restore path parses it back with the pure `parseSubagentTaskResult` (`webview-helpers.js`, subagent-only — a backgrounded shell command polls the same tool and is left as a normal row) and `maybeFinishSubagentFromTaskText` folds it into the card + drops the redundant poller row (else a restored background delegation showed a stuck card + a raw `[subagent:…]` row). **Failure/cancel are threaded through BOTH the tool-channel completion AND the lifecycle rail** (a completion routinely races ahead of the lifecycle, so the marker can't live only on one) — a failed card reads red (`.subagent-failed`), a cancelled one muted (`.subagent-cancelled`); `finishSubagentCard`'s already-done early-return upgrades a late status marker, not just a late duration. The restore-created card is marked `subagentReplayed` and excluded from **live** lifecycle matching (mis-attribution guard) but NOT during replay (`state.replaying || !replayed`), so it still consumes its own re-forwarded lifecycle. The `subagent_spawned`/`subagent_finished` lifecycle stream is delivered **two ways**: on the LIVE `_x.ai/session_notification` rail (→ `xaiNotification`; the pure `isSubagentLifecycleUpdate` in `acp-dispatch.ts` re-routes it to the same `subagentUpdate` channel the webview consumes) and on the persist/replay `_x.ai/session/update` rail (→ `subagentLifecycle` → `subagentUpdate`, for `session/load`). `subagent_finished` fills `duration_ms` + the child's output — the duration Composer's tool-channel completion lacks (probe-confirmed on 0.2.101; earlier 0.2.93 measurements watched only the persist rail, which never fires live, hence the old "the CLI only logs these" note). FIFO spawn-tagging + child-id matching keep the right card. A history replay settles never-completed rows at replay end. Child tool calls are **not** streamed on the parent connection — deliberately no nested inspector. grok persists each child as a **top-level sibling session** with `session_kind: "subagent"` — the history list hides those (`SessionListEntry.kind` from `buildEntry`, filtered in `postSessionsList`, which pages by index slots consumed — the `nextOffset` field on the `sessions` message — so hidden rows can't stall or duplicate load-more). Real wire capture: `test/fixtures/composer-subagent-session.jsonl` (replayed end-to-end by `test/subagent-replay.dom.test.ts`); deep-dive: `research/subagents.md`.
- **Logout (#13).** `grok.logout` command + gear-menu *Sign out* → `sidebar.logout()` runs `grok logout`, disposes the session, shows the auth-required onboarding.

## Known limits

- Subagent rows show the task + result, not the child's own tool calls — those are never streamed on the parent ACP connection (a nested inspector would have to read the child session from disk; deliberate non-goal)
- Generated media is served via `asWebviewUri` (streamed from disk) when it lives under the grok-home `localResourceRoot`; files outside that fall back to a base64 `data:` URI
- No worktree UI
- The native diff editor is preview-only: it reconstructs full-file context from the file on disk and Grok's replaced-region metadata, but falls back to the region alone if the file is unreadable, oversized, or no longer matches; the write happens via `fs/write_text_file` after approval
- View placement: **default-homed in the Secondary Side Bar** (`viewsContainers.secondarySidebar` — requires VS Code ≥1.106, the `engines` floor; older hosts can't install this build — **Antigravity is on base 1.104** and refuses it, keeping the last compatible version). Relocation is one-click via gear → Config & debug → **Move view** (direct `vscode.moveViews` into the per-location containers — `src/view-move.ts`), or drag. Cursor's primary-side-bar context menu still hides the built-in "Move To" entry — the gear mover is the workaround. User moves persist; a persisted placement wins over the contribution default (*Reset Location* restores the default)

## Cross-platform notes

- `terminal-manager.ts` selects the shell via `resolveTerminalShell(platform, resolve, pref)`: Windows runs the agent's commands under PowerShell (`pwsh.exe`→`powershell.exe`, else cmd.exe via `shell:true`) to mirror the standalone CLI (#46); everywhere else `shell:true` → `/bin/sh`. Node runs a string shell as `<shell> -c "<command>"` (pwsh + Windows PowerShell both accept `-c`, and Node passes the command string through faithfully — embedded double quotes / JSON / paths-with-spaces survive; only cmd/bash-style `\"` escaping doesn't, which grok won't emit for a PowerShell target). `whichOnPath` skips the `\WindowsApps\` Store-alias stub. `grok.terminalShell` (`"auto"`|`"cmd"`, host-read → `setTerminalShellPreference`, cache-clearing) is the escape hatch back to cmd. Don't hardcode shell paths. **The agent is told which dialect to WRITE for** via `GROK_SHELL` in its spawn env (`buildEnv` in `sidebar.ts` sets it from the pure `grokShellEnvValue(resolvedTerminalShell(), platform)` — `pwsh`/`powershell`/`cmd` on Windows, unset on POSIX, and never overriding a user-set value): grok's ACP mode otherwise describes the shell from its own host detection and can emit POSIX idioms for a PowerShell host (§2.9; the `Shell:` line in the model's first message tracks `GROK_SHELL`, `research/oss-surfaces-probe.cjs`). **The `pwsh`-vs-`powershell.exe`-fallback gap is real: Windows PowerShell 5.1 collapses any non-zero native exit to 1 (pwsh 7 preserves it) and rejects `&&`/`||` chains (pwsh 7 accepts them) — install pwsh 7 for full parity.** A fresh shell spawns per command, so the profile re-runs each time (matches standalone's reachable functions, but adds latency and any profile stdout can prepend to captured output; on a `Restricted` execution policy 5.1 skips the profile + warns). The agent only acts on zero-vs-non-zero, which is preserved. Root cause + probe evidence: `research/powershell-terminal.md`.
- `cli-locator.ts` reads `HOME` / `USERPROFILE` env vars first (testability), falls back to `os.homedir()`. Uses `where` on Windows, `command -v` elsewhere. Checks `.cmd`/`.exe`/`.bat` extensions on Windows.
- Tests use `node -e "..."` everywhere, so commands are deterministic across platforms — don't add `pwd`, `awk`, `sleep`, `true`, etc.

## What's next (priority order)

1. **Grow the `@vscode/test-electron` smoke** — activation/command/webview-resolve plus the **repo-selection split** (a remote `selectRepo` must move the REMOTE payload and leave the local one on the workspace root — verified by swapping the destinations and watching it fail while all 1397 unit tests still passed). The test-only hook now exists: `activate()` returns `{__test}` ONLY under `ExtensionMode.Test`, tapping `postLocal`/`postRemote` and injecting remote messages. Next: reuse it to inject a synthetic `session`/`historyReplay` and assert the webview renders it (diff editor, media serving).
2. Status-bar indicator (current model + effort + token usage)
3. Subagent extras (optional) — a nested child-activity inspector would read the child session from disk (`session_kind:"subagent"` siblings + `<parent>/subagents/<id>/meta.json`); deliberate non-goal for now
4. Worktree UI (`Grok: New Worktree Session`)
5. Optional: auto-move view to secondary side bar on first activation (`workbench.action.moveView`)

## Publishing

**Release procedure — ALWAYS tag + create a GitHub Release (with the `.vsix` attached) on a release push to `main`** (standing convention; mirrors the `v1.0.0…` tag history + GitHub Releases):

**The whole procedure below (steps 2–5) is scripted** — after bumping the version + writing the changelog section (step 1, user-initiated), just run:

```bash
pwsh scripts\release.ps1        # Windows (native) — what we use here
./scripts/release.sh            # macOS / Linux / WSL
```

It reads the version from `package.json`, runs the gate, builds the vsix, commits the working tree (`-MessageFile`/`-Message` override the default `Release vX.Y.Z`), pushes `main`, creates the annotated tag, and runs `gh release create` **with the vsix attached** — extracting the matching `## X.Y.Z` changelog section as the release notes. `-DryRun`/`--dry-run` previews; `-NoTest`/`--no-test` skips the gate. It refuses to run off `main` or when the tag already exists (i.e. the version wasn't bumped). The script does **not** call `vsce`/`ovsx`, and there is **no publish workflow** (`.github/workflows/` is just `ci.yml`) — so the GitHub Release does **not** auto-publish to any store. Store publishing is split by who runs it (step 6): the agent **always publishes Open VSX right after the script succeeds** (part of every release, no separate ask); the **VS Code Marketplace** is manual and Paweł-only.

What the script encodes, step by step:

1. Bump `version` in `package.json` (user-initiated) and add the dated section to `CHANGELOG.md`. As part of this pass, **review the release-facing docs for staleness against what actually ships** — this review is a required step, not optional:
   - `README.md` — features / screenshots / install matrix.
   - `TESTS.md` — test plan vs. what actually ships.
   - `CLAUDE.md` — the project map (§ Module map, § ACP surfaces, § Known limits, the test-count floor): does it still describe the current architecture? It deliberately **doesn't** hardcode the version (it points to the changelog), so it needs no version edit — but its prose can go stale as behavior changes.
   - `docs/architecture.md` — the architecture doc: keep it in sync with new modules / event flows / surfaces.
   - `package.json` itself — description, `engines`, `categories`, `contributes`.

   Keep `CHANGELOG.md` entries **terse** (1–3 bullets per release under Added/Changed/Fixed; deep detail belongs in commits / `research/`, not the changelog); releases before the current minor live in `docs/CHANGELOG-ARCHIVE.md` (linked from the foot of `CHANGELOG.md`).
2. `npm test` (1397-test floor, all green) + `tsc -p . --noEmit` clean + **`npm run test:integration`** (the real Extension Host — `npm test` is only CI's `test` job, and CI runs this as a SECOND required job; without it a release can be tagged and published before CI goes red), **and `npm run test:live` against real grok — mandatory, run without asking**. The `release.*` scripts **now run BOTH `test:integration` and `test:live` by default** as part of the gate (`-SkipIntegration`/`--skip-integration` and `-SkipLive`/`--skip-live` are the explicit escape hatches); running it by hand first is still wise since the scripts also commit/tag/push once it's green.
3. Commit + push to `main` (direct-to-main, no feature branches).
4. **Annotated git tag** `vX.Y.Z` at the release commit → `git tag -a vX.Y.Z -m "Release vX.Y.Z"` → `git push origin vX.Y.Z`.
5. **GitHub Release** for that tag → `gh release create vX.Y.Z --title "Release vX.Y.Z" --notes-file <notes> <vsix>` (notes = the new changelog section(s); include any earlier version that was bumped but never released). **Always attach the built `grok-vscode-phuryn-X.Y.Z.vsix` as a release asset** so the exact installable build is downloadable from the release.
6. **Store publish — split by store.**
   - **Open VSX: ALWAYS published by the agent as part of every release** (immediately after step 5, no separate ask) — `npx ovsx publish grok-vscode-phuryn-<version>.vsix -p <key>` with the key read from `OPEN_VSX_API_KEY` in the git-ignored `.env` at the repo root (never print or commit the key).
   - **VS Code Marketplace: manual, Paweł-only.** `npm run publish` (vsce; the `PawelHuryn` publisher is registered + authenticated locally). The agent never runs this — Paweł publishes it himself.
   - **Never announce a store as "live now."** A successful `ovsx publish` is NOT public availability — Open VSX scans new versions before listing them. In issue comments / user-facing messages, say **"~10 minutes"** for BOTH stores (Open VSX and the VS Code Marketplace alike).
   Publishing ≠ tagging either way.

Don't skip the tag/release (or the vsix asset) on a release push. (A pure mid-dev version bump that isn't a release — e.g. the unreleased v1.3.0 voice iteration — is the only exception.)

## Repo conventions

- **Work on `main`, synced to `origin/main`.** `main` is the only working line — start every task from `main` fast-forwarded to `origin/main`. The `dev` branch is **abandoned** (stale far behind `main`); do **not** develop on it. If the working tree is on `dev`, or `main` is behind `origin/main`, switch to `main` and `git reset --hard origin/main` before doing anything else (the published version lives on `main` — a stale checkout will look many versions behind and misnumber the changelog).
- Direct-to-`main`, no feature branches
- **Never commit or push automatically.** Changes reach `main`/`origin` only on the user's **explicit** request (rare) or as part of a release. Otherwise: build + reinstall locally for testing and leave the working tree uncommitted.
- Commits explain the *why*, not the *what*
- Don't introduce abstractions speculatively
- Don't add comments that explain what well-named code already says
- **Keep `CLAUDE.md` a current-state map** (see the editing-discipline note under § Status): rewrite changed behavior in place, never append changelog narrative or bug post-mortems here — link `research/*.md` / CHANGELOG instead
- 1435 tests is the floor — every PR should keep that green. All tests are grok-free (no binary spawn); grok-dependent probes live in `research/*.cjs` and are run manually, never by `npm test` or CI
- **Rebuilding clears older `.vsix` first** — `npm run package` (and the install/release scripts) delete stale `grok-vscode-phuryn-*.vsix` before building, so only the current version is on disk. After any doc or code change, rebuild + reinstall so the installed extension's bundled docs are current. **Package last:** the vsix bundles `CLAUDE.md`/`README.md`/`docs/` as files, so finish *all* doc + code edits **before** the final `npm run package` + reinstall — otherwise the installed build ships a stale-docs snapshot. If you rebuild mid-task and then touch docs again, rebuild again so the order is always edit-everything → package → reinstall.
- **Version bumps are user-initiated.** Iterate at the current version (rebuild the same vsix and reinstall locally) until the user says to bump and publish. Don't bump `package.json` on your own. When Paweł asks for a release, the default is a **patch** bump (+0.0.1); **any minor or major bump requires his direct, explicit approval** — never infer it from the size of the change.
- **Releasing requires a direct instruction to release — a version number alone is never one.** Only an unambiguous imperative ("release it", "publish 2.0.3", "run the release script") authorizes the § Publishing procedure. Naming a version in any other role does **not**: *"tell them it ships in 2.0.3"*, *"put this under 2.0.3 in the changelog"*, *"this would be a 2.0.3"* are all scope/labelling statements, and treating one as an authorization ships an unrequested tag, GitHub Release, and Open VSX publish — three externally-visible, effectively irreversible actions. The same applies to a release named inside a larger request: the imperative must attach to *releasing*, not to the work. Default to doing the work, leaving the tree uncommitted, and reporting it as ready to release. **In any doubt, ask** — a one-line question costs nothing next to an unrequested publish.
- **Sign GitHub comments.** Every GitHub issue/PR comment posted on the user's behalf ends with a signature on its own final line (italic). Pick by whether Paweł actually reviewed the text before it was posted: if he reviewed it, use `_Written with an agent, reviewed by Paweł_`; if the agent posted it without his review, use `_Written by Pawel's agent_`. Only claim review when it actually happened.
- **The `phuryn` GitHub account is Paweł** — the user you are working with and the repo owner. Unsigned comments from `phuryn` are his own words (don't read them as a third party's); agent-posted comments carry the signatures above.
